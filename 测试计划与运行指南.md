# DoVideoAI 测试计划与运行指南

## 1. 环境准备

### 1.1 前置检查

```powershell
# 确认 JDK 21
$env:JAVA_HOME = "D:\soft\Java\jdk-21"
java -version
# 期望: java version "21.0.4" 2024-07-16 LTS

# 确认 Maven
mvn --version
# 期望: Java version: 21.0.4

# 确认 Node.js
node --version
# 期望: v20.19.5+

# 确认 Docker
docker --version
```

### 1.2 启动中间件（Docker Compose）

```powershell
cd d:\github\DOVideo-AI
docker-compose up -d
```

验证所有容器已启动：

```powershell
docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"
```

期望看到以下容器均处于 `Up` 状态：

| 容器 | 端口 | 用途 |
|------|------|------|
| mysql | 3307 | 数据库 |
| redis | 6379 | 缓存 / Token 限额 |
| minio | 9000, 9001 | 对象存储 |
| rocketmq-namesrv | 9876 | MQ 注册中心 |
| rocketmq-broker | 10911 | MQ 消息代理 |

---

## 2. 启动后端

```powershell
cd d:\github\DOVideo-AI\server

# 编译 + 测试
$env:JAVA_HOME = "D:\soft\Java\jdk-21"
mvn clean test

# 启动
mvn spring-boot:run
```

**启动成功标志**：控制台输出 `Started DOVideoApplication in x.xxx seconds`

> 注意：中间件必须先启动，否则后端会连不上 MySQL/Redis/MQ 而报错退出。

---

## 3. 启动前端

```powershell
cd d:\github\DOVideo-AI\client
npm install
npm run dev
```

**启动成功标志**：控制台输出 `Local: http://localhost:5173/`

打开浏览器访问 [http://localhost:5173/](http://localhost:5173/)

---

## 4. 功能测试

### 4.1 用户注册与登录

| 步骤 | 操作 | 期望结果 |
|------|------|----------|
| 1 | 点击右上角「登录 / 注册」 | 弹出登录面板 |
| 2 | 点击「去注册」 | 切换到注册模式 |
| 3 | 输入 username / password / nickname | — |
| 4 | 点击「提交注册」 | 提示"注册成功，请直接登录" |
| 5 | 输入账号密码，点击「立即登录」 | 右上角显示 `:: 你的昵称 ::`，并提示"欢迎回来" |

**验证点**：MySQL `user` 表中新增一行用户记录。

```sql
SELECT * FROM `user` ORDER BY id DESC LIMIT 1;
```

---

### 4.2 本地上传视频

| 步骤 | 操作 | 期望结果 |
|------|------|----------|
| 1 | 点击左侧"LOCAL FILE"区域 | 打开文件选择器 |
| 2 | 选择一个视频文件（建议 <100MB 测试用小视频） | — |
| 3 | 观察上传状态 | 状态条显示"数据传输中..."，完成后显示 "✅ 本地上传完成" |
| 4 | 查看工作台 | 出现一张新的卡片，状态为 `PROCESSING` → `READY` |

**验证点**：
- MinIO Console [http://localhost:9001](http://localhost:9001)（账号 `minioadmin` / `minioadmin`）中 `media` bucket 出现该文件。
- MySQL `media_files` 表中新增一行。

```sql
SELECT id, filename, status, file_path FROM media_files ORDER BY id DESC LIMIT 1;
```

---

### 4.3 链接上传（B站/YouTube）

| 步骤 | 操作 | 期望结果 |
|------|------|----------|
| 1 | 在右侧"WEB LINK"区域的输入框粘贴一个视频链接 | — |
| 2 | 点击箭头按钮或按 Enter | 状态条显示"正在解析链接..." |
| 3 | 等待下载完成 | 提示"✅ 链接资源已入库" |

**异常场景**：

| 测试 | 操作 | 期望结果 |
|------|------|----------|
| 空链接 | 不输入链接直接点击按钮 | 无反应 |
| 非法格式 | 输入 `ftp://xxx` | 提示"请输入合法的 http/https 链接" |
| 不支持的平台 | 输入某小众视频站链接 | 提示"不支持该平台链接" |

---

### 4.4 音频提取（文字转录）

| 步骤 | 操作 | 期望结果 |
|------|------|----------|
| 1 | 在工作台找到状态为 `READY` 的卡片 | — |
| 2 | 点击「提取文字」按钮 | 右侧弹出侧栏，显示"📝 提取任务已提交..." |
| 3 | 等待 3~30 秒 | 侧栏自动刷新显示全量转写文本 |
| 4 | 观察页面顶部 | 弹出 "✅ 任务完成" |

**验证点**：
- MySQL `media_files.transcript_text` 字段不为空。
- 再次点击「提取文字」不会重复提交（已有结果直接展示）。

---

### 4.5 AI 智能总结 + 时间轴导览（核心亮点一、二联动）

#### 4.5.1 正常流程

| 步骤 | 操作 | 期望结果 |
|------|------|----------|
| 1 | 工作台中找一张状态为 `READY` 的卡片 | — |
| 2 | 点击「AI 智能总结」按钮 | 右侧弹出侧栏，显示"🚀 正在向分布式集群请求计算资源..." |
| 3 | 稍等片刻 | 侧栏显示"✅ 任务已投递至 RocketMQ！⏳ 等待消费者接单处理..." |
| 4 | 等待 10~60 秒（取决于视频长度和 API 响应速度） | **侧栏出现时间轴 UI**：上方为视频播放器，下方为可点击的时间轴节点列表 |
| 5 | 点击时间轴中任意节点（如 `2:00 分布式锁原理`） | 视频跳转到对应秒数并自动播放 |

**期望的 AI 返回格式**（JSON 数组）：

```json
[
  {"startTime": 0, "topic": "开场背景介绍", "summary": "介绍本期视频的问题背景和主要讨论方向。"},
  {"startTime": 120, "topic": "分布式锁原理", "summary": "解释分布式锁如何协调多实例并发访问共享资源。"},
  {"startTime": 240, "topic": "Redis 实现方式", "summary": "使用 Redis SETNX 结合 Lua 脚本实现原子性锁操作。"}
]
```

**验证点**：
- 时间轴 UI 正常渲染（左侧竖线 + 圆点 + 时间段 + 标题 + 摘要）。
- 鼠标悬停时间轴节点时，背景变亮、轻微右移。
- 视频播放器能正常加载并播放。
- 非 JSON 结果（如旧版 Markdown）仍然走 Markdown 渲染兜底。

#### 4.5.2 重复提交防护

| 步骤 | 操作 | 期望结果 |
|------|------|----------|
| 1 | 对一张已提交 AI 分析但**尚未完成**的卡片，再次点击「AI 智能总结」 | 侧栏提示"🚀 系统正在后台拼命计算中...（任务正在进行，无需重复提交）" |

#### 4.5.3 已有结果直接展示

| 步骤 | 操作 | 期望结果 |
|------|------|----------|
| 1 | 对一张**已完成** AI 分析的卡片，再次点击「AI 智能总结」 | 直接展示结果，无需再次请求后端 |

---

### 4.6 Token 成本核算与限流（核心亮点一）

#### 4.6.1 Token 记账验证

**前提**：已至少完成一次 AI 智能总结（4.5 步骤），大模型 API 成功返回。

| 步骤 | 操作 | 期望结果 |
|------|------|----------|
| 1 | 连接 Redis CLI | `docker exec -it dovideoredis redis-cli` |
| 2 | 查询 Token 用量 | `GET user:token:usage:1`（1 替换为实际 userId） |
| 3 | 检查 TTL | `TTL user:token:usage:1` |

期望：
- 返回值 > 0（例如 `1234`，表示累计消耗 Token 数）。
- TTL 在 0 ~ 86400（24h）之间。

#### 4.6.2 配额拦截验证

**测试方法**：将日配额调低至 1，模拟配额耗尽。

1. 修改 `server/src/main/resources/application.properties`：

```properties
ai.token.daily-quota=1
```

2. 重启后端。

3. 在 Redis 中手动写入超过阈值的用量：

```bash
docker exec -it dovideoredis redis-cli
SET user:token:usage:2 "50001"
# 2 替换为你的测试 userId
```

4. 用该用户登录前端，点击「AI 智能总结」。

**期望结果**：
- 页面顶部弹出红色通知："今日 AI 算力已耗尽"。
- 侧栏不打开 / 自动关闭。
- HTTP 响应状态码 429（可在浏览器 DevTools Network 面板验证）。

5. **恢复配置**：改回 `ai.token.daily-quota=50000` 并重启后端。清除 Redis 测试数据：

```bash
DEL user:token:usage:2
```

#### 4.6.3 匿名用户不限额

| 步骤 | 操作 | 期望结果 |
|------|------|----------|
| 1 | 不登录，直接通过 API 调用 `/debug/ai?id=1` | 不返回 429（因为匿名用户的 userId 为 null，`hasQuota(null)` 返回 true） |

#### 4.6.4 24 小时自动重置

| 步骤 | 操作 | 期望结果 |
|------|------|----------|
| 1 | 触发一次 AI 分析，确认 Redis Key 已写入 | — |
| 2 | 手动设置 TTL 为 10 秒 | `EXPIRE user:token:usage:{userId} 10` |
| 3 | 等待 10 秒后再次查询 | Key 已过期消失 |
| 4 | 再次发起 AI 分析 | 不会被配额拦截，且新 Key 自动带上 24h TTL |

---

### 4.7 数据库验证

#### ai_summary_result 表

```sql
SELECT * FROM ai_summary_result ORDER BY id DESC LIMIT 3;
```

期望：每次 AI 总结完成后新增一行，`result_json` 字段存储原始 JSON 字符串。

#### media_files 表

```sql
SELECT id, filename, status, transcript_text, ai_summary
FROM media_files ORDER BY id DESC LIMIT 3;
```

期望：
- `transcript_text` 在文字提取完成后不为空。
- `ai_summary` 在 AI 总结完成后包含 JSON 数组。

---

### 4.8 异常场景测试

| 测试场景 | 操作 | 期望结果 |
|----------|------|----------|
| 未登录上传 | 不登录，点击上传区域 | 弹出"⚠️ 权限受限：请先登录系统" |
| 未登录链接 | 不登录，粘贴链接提交 | 弹出"⚠️ 权限受限：请先登录系统" |
| 上传超大文件 | 上传 > 2GB 文件 | 前端或后端拒绝（取决于配置 `max-file-size`） |
| 删除文件 | 点击卡片右上角 × | 弹出确认框 → 确认后卡片消失 |
| 下载音频 | 点击「下载音频」 | 浏览器触发 MP3 文件下载 |
| 重复提交 | 快速连续点击 AI 总结 | 第二次提示"任务提交中，请勿重复点击"（Redisson 锁） |
| 全局限流 | 1 分钟内超过 10 次 AI 请求 | 提示"⚠️ 系统繁忙(限流中)，请 1 分钟后再试" |
| Token 超限 | 当日累计 Token 超过 50000 | 提示"今日 AI 算力已耗尽" |
| 后端宕机 | 后端未启动时访问前端 | 工作台为空，上传报 Network Error |

---

## 5. 一键脚本

将以下内容保存为 `run-test.ps1`：

```powershell
# ===== 环境变量 =====
$env:JAVA_HOME = "D:\soft\Java\jdk-21"
$env:Path = "$env:JAVA_HOME\bin;$env:Path"

Write-Host "=== 1/4 启动中间件 ===" -ForegroundColor Green
cd d:\github\DOVideo-AI
docker-compose up -d
Start-Sleep -Seconds 5
docker ps --format "table {{.Names}}\t{{.Status}}"

Write-Host "=== 2/4 编译后端 ===" -ForegroundColor Green
cd d:\github\DOVideo-AI\server
mvn clean package -DskipTests
if ($LASTEXITCODE -ne 0) { Write-Host "后端构建失败！" -ForegroundColor Red; exit 1 }

Write-Host "=== 3/4 运行测试 ===" -ForegroundColor Green
mvn test

Write-Host "=== 4/4 构建前端 ===" -ForegroundColor Green
cd d:\github\DOVideo-AI\client
npm run build
if ($LASTEXITCODE -ne 0) { Write-Host "前端构建失败！" -ForegroundColor Red; exit 1 }

Write-Host "`n===== 全部就绪 =====" -ForegroundColor Green
Write-Host "启动后端: cd server; mvn spring-boot:run" -ForegroundColor Cyan
Write-Host "启动前端: cd client; npm run dev" -ForegroundColor Cyan
Write-Host "访问地址: http://localhost:5173" -ForegroundColor Cyan
```

---

## 6. 测试检查清单

| # | 功能 | 测试通过? |
|---|------|-----------|
| 1 | 用户注册 | ☐ |
| 2 | 用户登录 | ☐ |
| 3 | 本地上传视频 | ☐ |
| 4 | 链接上传视频 | ☐ |
| 5 | 工作台列表展示 | ☐ |
| 6 | 音频提取（文字转录） | ☐ |
| 7 | 音频下载 | ☐ |
| 8 | 删除文件 | ☐ |
| 9 | AI 智能总结 | ☐ |
| 10 | 时间轴 JSON 解析 | ☐ |
| 11 | 时间轴节点点击跳转播放 | ☐ |
| 12 | 时间轴非 JSON 降级 Markdown | ☐ |
| 13 | Redis Token 记账 | ☐ |
| 14 | Token 配额拦截 (HTTP 429) | ☐ |
| 15 | Token 配额弹窗提示 | ☐ |
| 16 | 24h TTL 自动重置 | ☐ |
| 17 | 分布式锁防重复提交 | ☐ |
| 18 | 全局 QPS 限流 | ☐ |
| 19 | 未登录拦截 | ☐ |
| 20 | `ai_summary_result` 表写入 | ☐ |
