

我个人简历上项目
SmartVideo Engine 智能视频解析平台
技术栈:Spring Boot, MySQL, Redis, Redisson, RocketMQ, MyBatis-Plus, MinIO, OkHttp, Vue
项目简介: 面向学习场景的全链路视频内容理解平台,支持本地视频/URL上传、分片断点续传、语音转写与 AI 时间轴总结,以可交互时间轴呈现分析结果。针对大文件弱网上传、长耗时阻塞、重复提交及 Token 成本失控等痛点,基于 Spring Boot + Redis + MinIO + RocketMQ + Redisson 进行架构重构,实现上传链路可靠化、分析链路异步化与调用成本风控。
・实现 5MB 分片上传与断点续传,前端计算文件 MD5 与分片 MD5,后端以 Redis Set 记录已上传分片并设置 24h TTL,分片先落 MinIO 再记账。合并阶段通过 Redis SETNX 合并锁 + MySQL file_md5 唯一索引兜底,避免重复合并与重复存储。
・引入 RocketMQ 将语音转写与 AI 推理链路异步化,AI 分析提交接口只负责加锁、限流、更新排队状态并投递消息,接口响应从 60s+ 压缩至 50ms 级。消费者处理 FFmpeg 音频提取、ASR 转写与 DeepSeek 总结,异常抛给 RocketMQ 最大 3 次重试,失败后进入 DLQ 并标记任务失败。
·基于 Redisson + WatchDog 实现双阶段任务锁：提交阶段在 /debug/ai 使用 tryLock(0, -1, TimeUnit.SECONDS) 防连点,投递 MQ 后立即释放；消费者阶段在 AiService.asyncAnalyze 使用 tryLock() 持有整个处理过程,由 WatchDog 自动续期,避免重复消息并发执行导致重复 ASR/LLM 调用。
·自研 AiTokenQuotaInterceptor 拦截器,在 /debug/ai、/debug/ai-direct、/debug/transcribe 入口做用户日额度校验；DeepSeek 返回 usage 后通过 Redisson RAtomicLong 累计 user:token:usage:{userId},首次写入设置 24h TTL,超额时统一返回 429。配合 Redisson RRateLimiter 实现 AI 分析 10次/min 全局限流,前端 Token 进度条实时展示消耗。
·通过结构化 Prompt 约束 DeepSeek 输出 startTime/topic/summary JSON 数组,后端持久化到 media_files.ai_summary 与 ai_summary_result.result_json,前端解析 JSON 渲染为可交互时间轴。点击节点驱动 videoElement.currentTime 跳转播放,将长视频转化为按知识点索引的导览体验。


以下问题并非实际，更多是面向面试我被拷打多了所总结的最优打法..
其实学的多的完全可以进一步扩充答案，我从正式接触java都不到俩月，所以总结的很保守，牢牢控制面试在我能答上来的区间内。被问穿发现其实说的东西根本不会是极大扣分项。反之则是加分项，大家可以举一反三。

## 项目概述

项目介绍
      SmartVideo Engine 是我借助 AI Coding 辅助开发的全栈项目，本质上是一个面向学习场景的视频内容理解平台。用户可以上传本地课程视频，也可以提交 URL 让后端用 yt-dlp 下载入库；上传完成后，可以触发语音转写和 AI 总结，系统会把长视频内容整理成带 startTime 的时间轴 JSON，前端再渲染成可点击的知识点导览。
      项目做好基础功能之后，我发现核心的问题不是“能不能调通 AI”，而是视频处理天然长耗时、文件大、弱网下容易失败，而且 AI 调用有真实 Token 成本。所以我围绕真实使用链路做了几类工程化改造：大文件分片断点续传、AI 分析异步化、重复提交拦截、Token 额度统计和全局限流。
      目前代码也开源在GitHub上，上线不到两个月可以获得了60多个Star，虽然star不多，但是能说明我这个项目是能解决一些实际需求或者可借鉴的。

1. 比如在上传课程平台的视频时，由于校园网不稳定，大文件一旦中断就要重传，我引入了 5MB 分片上传、MD5 校验、Redis 记录已传分片和前端主动 merge 的断点续传机制。
2. 比如用户端重复点击“AI 分析”会导致同一 mediaId 多次投递 MQ，最后产生重复 ASR/LLM 调用，所以我在提交入口加了 Redisson 分布式锁，并配合数据库状态字段做二次拦截。
3. 比如 AI API 是按 Token 计费的，我加了日维度 Token 额度统计、429 熔断和 Redisson RRateLimiter 全局限流，避免测试或误操作把额度打爆。



## 项目真实性准备

Q1: 全项目架构与流程？
第一阶段是：上传与存储。
小文件走 /media/upload 直传 MinIO 并写入 media_files；大于 5MB 的文件走分片上传。前端先用 spark-md5 计算整文件 MD5，再按 5MB 切片，并发数控制为 3。上传前请求 /media/chunk/check，后端先查 MySQL 的 file_md5 唯一索引实现秒传，再查 Redis Set upload:task:{md5} 返回已传分片。单片上传时前端携带 chunkMd5，后端校验通过后先把分片落到 MinIO，再把 chunkIndex 写入 Redis Set，并刷新 24h TTL。
第二阶段是：合并与入库。
前端确认所有分片请求都成功后，主动调用 /media/chunk/merge。后端用 Redis SETNX merge:lock:{md5} 做 30 秒合并锁，防止前端超时重试导致重复合并；随后再次查 MySQL 防重复，校验 Redis 中已上传分片数等于 totalChunks，再调用 MinIO composeObject 合并完整视频，最后写入 media_files 的 file_path、file_md5、file_size、chunk_count 等字段，并清理 Redis 分片状态和 MinIO 临时分片。
第三阶段是：提交分析与异步削峰。
用户点击 AI 分析时，请求进入 /debug/ai。这个入口先用 Redisson RLock(lock:analyze:{mediaId}) 防连点，再用 Redisson RRateLimiter(limit:ai:global) 做 10次/min 全局限流；同时 AiTokenQuotaInterceptor 会按 userId 检查当天 Token 额度。通过后，后端把 media_files.ai_summary 更新为“已进入消息队列”，清理用户列表缓存，并向 RocketMQ 的 video-analysis-topic 投递 AnalysisTaskMsg，接口立刻返回，释放 Web 线程。
第四阶段是：消费者处理与容错。
VideoAnalysisConsumer 收到消息后先把任务标记为处理中，然后调用 AiService.asyncAnalyze。业务内部先基于 mediaId 获取 Redisson 锁 lock:analyze:{mediaId}，tryLock() 抢不到说明已有消费者在处理同一任务，直接跳过；抢到锁后在锁内二次查询 DB，防止上一个持有者刚处理完。随后检查用户 Token 额度，用 FFmpeg 提取音频，上传临时 mp3 到 MinIO 并生成 1 小时预签名 URL 供 ASR 访问，调用阿里 ASR 得到转写文本，再调用 DeepSeek 生成结构化时间轴总结。DeepSeek 返回 usage 后会通过 TokenUsageService 记录 Token 消耗。总结结果同时写入 media_files.ai_summary、media_files.transcript_text 和 ai_summary_result.result_json。异常会向上抛给 RocketMQ 重试，maxReconsumeTimes=3；仍失败时进入 %DLQ%video-group，由 DLQ 消费者把任务标记为失败提示。finally 中清理 TokenUsageContext，并在当前线程持有锁时 unlock，停止 WatchDog 续期。
第五阶段是：前端轮询和时间轴渲染。
前端拿到“任务已投递”的响应后，每 3 秒刷新 /media/list 查询该 mediaId 的 aiSummary 或 transcriptText。检测到结果包含 JSON 时间轴后停止轮询，parseTimeline 解析 startTime/topic/summary 并渲染时间轴；用户点击节点时先请求 /media/access/{id} 获取 1 小时有效的预签名视频 URL，再设置 videoElement.currentTime 并播放，实现按知识点跳转。
[图片]


---

Q2: 前端代码生成的具体流程
因为我的求职意向是 后端开发 ，在这个项目中，我把 90% 以上的精力都投入到了后端的架构设计。 前端对我来说，更多是作为一个可视化验证平台，用来直观地展示我后端业务流程的闭环。所以前端主要是通过AI辅助生成的，但我在这个过程也不是随便问一句就能用，我自己也摸索出了一套比较高效的流程。

1. 整理后端接口文档
我是后端写完接口之后才做前端的，所以我会先把后端的接口文档整理成一份Md文件——每个接口的路径、请求方法、参数格式、返回结构全部写清楚。这份.md文档既是我自己的开发记录，也是给AI的核心上下文。因为我开始试过不给接口文档直接让AI自己写前端，它会自己编造接口路径和参数格式，后面联调全是问题。 
2. 按模块拆分生成
我不会让AI一次性生成整个前端，那样代码量太大、质量非常不可控。我是按功能模块拆的，这个项目大概拆了四个模块：分片上传组件、每个视频的状态展示、文字提取板块、总结板块，每个模块单独让AI一步步生成。
3. 按模块review
AI生成的代码我必须去按模块检查，尤其关注接口对接是否正确、异常处理是否完善、用户交互逻辑是否合理，发现问题再针对性地让AI修改。
4. DUBUG
哪怕这么谨慎，哪怕我加价用claude Opus4.6，前后端联调的时候还是绝大概率会有问题，比如:
- 比如数据格式不匹配，后端返回的时间戳格式和前端预期的不一样。
- 比如异常场景没覆盖，AI分析接口超时后前端没有任何反馈，一直转圈。
出现这些问题我会再具体的报错信息和现象去让AI针对性地改。
具体细节到每个模块，我也会给AI三个约束：
技术约束（Vue3 + 什么UI框架）、
接口约束（直接把对应的接口文档贴进去）、
交互约束（比如上传组件要支持拖拽、要显示分片进度条、上传失败要有重试按钮）。


---

Q3: AI Coding开发方法论
1. 需求拆解，这个阶段不写任何代码。 
我会把项目目标描述给AI，和它讨论问题：系统应该拆成哪些模块？每个模块之间怎么交互？有哪些我没想到的技术风险？比如我最初的想法就是"上传视频→转文字→AI总结"，一个很简单的串行流程。但和AI讨论后，它帮我识别出几个我没想到的问题——大文件上传的堵塞风险，第三方API有调用频率限制。这些问题直接改变了我的方向，从一个简单的同步流程变成了现在的架构。
2. 技术选型验证。 
架构确定后，每个具体技术点我会让AI给出候选方案并对比，但我不会直接采纳，会去官方文档交叉验证。因为AI有幻觉问题，我踩过坑——AI曾经推荐我一个不存在的MinIO API方法。
比如锁选型，我没有在所有地方都硬套 Redisson。分片合并只是一个短临界区，所以我用 Redis SETNX + 30 秒 TTL 就够了；AI 分析链路分成两把 Redisson 锁：提交入口锁防用户连点导致重复投递 MQ，消费者锁覆盖 FFmpeg、ASR、DeepSeek 和落库的完整长任务，核心原因是 WatchDog 自动续期适合这种耗时不可预估的任务。这个选型理由是我基于自己的业务场景拆分出来的。
3. 分模块实现。 
先定义规范，再让AI在框架内编码。我会先在.md文件里写好这个模块的接口定义、输入输出格式、异常处理要求，把这些作为上下文约束喂给AI。AI在这些约束下生成代码，我来review核心逻辑、跑测试验证。前端也是同样的思路，把后端接口文档作为约束条件提供给AI，按模块逐个生成和验证。
4. DUBUG
哪怕这么谨慎，哪怕我加价用claude Opus4.6，具体实践的时候还是绝大概率会有问题，比如:
- 比如数据格式不匹配，后端返回的时间戳格式和前端预期的不一样。
- 比如异常场景没覆盖，AI分析接口超时后前端没有任何反馈，一直转圈。
出现这些问题我会再具体的报错信息和现象去让AI针对性地改。
AI当高级搜索引擎和代码助手，而不是当项目经理。 需求理解、架构决策、质量把关始终是我自己做的。

---

Q4: API选型考虑
我的项目需要两类 AI 能力：语音转文字和文本总结。当前真实代码里是“阿里 ASR + DeepSeek Chat”的组合：后端先用 FFmpeg 从视频中提取 mp3，上传临时音频到 MinIO，交给阿里 ASR 做转写；拿到转写文本后，再调用 DeepSeek 的 /chat/completions 生成时间轴总结。
OpenAI 我考虑过，模型能力强，但网络和成本都不适合作为这个项目的默认方案。
ASR 没有直接让 DeepSeek 做，是因为视频语音转写属于专门能力，阿里 ASR 对中文课堂音频的稳定性和接入方式更适合当前项目。
总结阶段选 DeepSeek，是因为它成本相对可控，中文总结效果够用，而且可以通过结构化 Prompt 约束输出 JSON，前端能直接解析成时间轴。
TPM是Tokens Per Minute的限制，就是平台对每分钟可以处理的Token总量有上限。
我目前没有遇到过平台 TPM 超限，因为用户量小。但我在设计上考虑了成本和频率问题：提交入口用 Redisson RRateLimiter 做 10次/min 全局限流，RocketMQ 的异步消费也天然起到削峰作用；如果未来用户量增长，我会在消费者侧增加按模型 TPM 计算的消费速率控制，而不是只在 Web 入口限流。

---

Q5: 你项目中的难点体现在哪
这个项目的功能实现本身不复杂，我觉得核心难点在于怎么让它在真实使用场景下稳定可靠地运行。
最初处理视频的一系列操作都是同步执行的，这导致用户界面的接口响应时间经常长达一分钟以上，而且极其容易因为网络问题导致整个请求失败，同时也严重阻塞了后端的资源。
为了突破这个瓶颈，我引入了RocketMQ对核心链路进行解耦。我将视频分片合并落盘后的处理任务全部发送到消息队列异步执行。前端点击上传后，核心接口的响应时间成功压缩到了50毫秒以内。
但异步化只解决了后端的阻塞问题，随之暴露了第二个难点。对于GB级别的大文件，纯粹的网络传输阶段在弱网环境下极易发生中断。一旦断开，用户必须从零开始重新上传，体验非常糟糕。
顺着这个痛点，我设计并落地了分片上传与断点续传机制。我在前端将大文件按5MB切片，后端基于Redis的Set数据结构记录上传成功的分片序号。这就保证了在网络不稳定的情况下，系统下次只传输缺失的数据块，彻底解决了大文件传输的可靠性与体验问题。
字节：这些不都是AI帮你解决的吗？
AI帮我加速了执行过程，但问题的识别和方案的判断是我自己做的。比如锁选型，AI给了我 Redis SETNX、Redisson、数据库唯一约束等方案，但它不会自动区分“分片合并短临界区”和“AI 分析提交入口”这两种场景。最终我选择合并阶段用 SETNX，提交阶段用 Redisson WatchDog，这是基于代码链路和故障边界自己拆出来的。再比如容错链路，AI可以帮我写重试代码，但“入口限流、MQ 重试、DLQ 兜底分别拦截什么层次的故障”这个设计思路是我根据实际链路总结出来的。

Q6: 项目价值与产品思维 (背景，腾讯问的
这个项目的起点是我自己的一个真实痛点。我们学校有个课程回放平台叫"学在吉大"，期末周大家都会去下载课程视频复习。但视频动辄一两个小时，从头看效率很低，而且那个平台本身体验也很差。我就想能不能把视频自动转成文字，再用AI帮忙提炼重点，这样复习效率会高很多。
后来我把代码开源到GitHub上，两个月内拿到了60多个Star，这可以说明这个需求不只是我一个人的痛点。
目前没有上线到公网，主要是成本考虑——AI接口调用是按Token收费的，没有商业模式的情况下我没法承担大规模用户的开销。当前我已经做了单用户日维度 Token 额度、429 熔断和 10次/min 全局限流；如果要推向更大范围，我觉得还需要补几个东西：
一是支持通过URL直接解析视频而不用手动下载上传，降低使用门槛；
二是引入 RAG 做更精准的内容问答；三是把现在的“技术限额”升级成真正的产品化计费体系，比如按套餐、额度包和账单记录来管理成本。


Q7: 0到1使用AI解决问题的具体案例
我举一个分片上传里遇到的并发问题。
最初的设计是：每个分片上传成功后，后端检查Redis里已完成的分片数是否等于总数，够了就自动触发合并。这个逻辑看起来没问题，我单线程测试也是正常的。
但上线给同学用之后出了问题。 我在日志里发现MinIO报了合并冲突的错误。排查后定位到原因：并发上传时，最后几个分片几乎同时完成，多个线程同时检测到"分片凑齐了"，于是同时向MinIO发起合并指令，导致状态机冲突。
我把这个问题连同代码和报错日志一起丢给了AI。 AI给了我三个方向的建议：一是在合并操作前加分布式锁，只有抢到锁的线程执行合并；二是用Redis的INCR原子计数，只有计数器恰好等于总分片数的那个请求触发合并；三是把合并的决策权交给前端，前端确认所有分片都返回200后，统一发起一次合并请求。
我没有直接采纳AI的第一个建议，虽然加锁能解决问题，但这意味着每个分片上传完都要去抢一次锁，增加了不必要的开销。第二个方案INCR计数器看起来很优雅，但有个隐患——如果某个分片上传成功了但INCR失败了，计数器永远凑不齐，合并就永远不会触发。
最终我选了第三个方案：前端当总指挥。 前端发出所有分片请求，等全部返回成功后，主动且唯一地调用一次/merge接口。后端的/merge接口再做一层幂等保护——用SETNX设合并锁，防止前端因网络超时重复调用。这样从根源上杜绝了并发合并的问题。
这个案例让我体会到AI最大的价值不是直接给答案，而是帮你快速展开方案空间。 它一次性给了三个方向，但每个方案在我的具体场景下适不适合，需要我自己去分析利弊。最终的技术决策还是基于我对自己业务场景的理解做出的。

---

Q8: 实际AI感受到的不足之处
我在深度使用中感受到几个主要不足：
第一，长上下文遗忘。 当对话轮次多了之后，AI容易忘记前面约定的技术方案或约束条件，导致后面的代码和前面矛盾。我的应对方式是把关键约定写成独立的文档，每次对话时作为上下文输入。
第二，幻觉问题。 AI有时会编造不存在的API或者库，尤其是在不太主流的技术栈上。所以AI生成的代码必须经过人工验证，不能直接信任。
第三，调试能力有限。 遇到比较复杂的、涉及多个组件交互的bug，AI往往需要大量的上下文才能定位问题，效率反而不如自己debug。

---
Q9: 更前沿的AI Coding方式？
我有关注这个方向。目前AI参与研发流程正在从"人工辅助编码"演进到"Agent的端到端开发"。像Claude Code这类工具已经能做到比较深度地参与代码编写，包括从0理解项目上下文、跨文件去修改等。更前沿的方向像多Agent协作，把需求分析、编码、测试拆给不同的Agent来做。
我有一直去关注这些方面的概念，但确实还没有实际的机会实践，这是我下一个方向的目标。


Q10: 新兴agent关键词，结合项目
Function Calling： 让大模型在对话中自主决定调用哪个函数来获取信息或执行操作。当前项目没有落地 Function Calling，真实落地的是结构化 Prompt 生成时间轴；如果后续做追问，可以把“查询转写片段”“查询处理状态”做成工具。
RAG（检索增强生成）： 先从知识库里检索相关内容，再把检索结果和用户问题一起喂给模型，减少幻觉。当前项目没有落地 RAG，下一步可以把转写文本按时间切片后存入向量数据库，用户追问时先召回相关片段，再让模型回答。
Agent： 和Function Calling的区别是，Function Calling是模型被动选择一个工具，Agent是模型主动规划多步任务并依次执行。比如用户说"总结第三章"，Agent会自主规划：定位时间段→提取文字→调用总结→返回结果。
MCP（Model Context Protocol）： 可以理解为AI调用外部工具的统一接口标准，类似USB。任何工具实现了这个协议，就能被支持MCP的模型调用。
Vibe Coding（氛围编程）： 最近很火的概念，指的是开发者用自然语言描述需求，让AI来写绝大部分代码，开发者主要做方向引导和质量把关。其实你做这个项目的过程就很像Vibe Coding，只是你没有用这个词来描述。
Multi-Agent（多智能体协作）： 多个Agent各自负责不同角色（产品经理Agent、程序员Agent、测试Agent），协作完成一个完整的开发任务。
Context Engineering（上下文工程）： 最近取代Prompt Engineering的新概念。不只是优化提示词本身，而是优化喂给模型的整个上下文——包括系统提示、相关文档、历史对话、工具定义等，让模型在最合适的信息环境下工作。我在用Claude Code时把接口文档和目录结构一起提供给AI，本质上就是在做Context Engineering。
Agentic Coding（智能体编程）： 让AI Agent端到端地参与软件开发全流程，从需求分析到编码到测试。Claude Code、Cursor Agent Mode都属于这个方向。

Q11: 项目中编写的skills
这个项目里真正落地的 MQ 消费者主要是 VideoAnalysisConsumer 和 VideoAnalysisDlqConsumer。主消费者负责解析 mediaId、标记处理中、调用 AiService 完成 FFmpeg 提取音频、ASR 转写、DeepSeek 总结和落库；DLQ 消费者负责在 RocketMQ 重试耗尽后解析消息体并把任务标记为失败。
我把这类消费者开发规范沉淀成了 CLAUDE.md / Skill 形式，重点不是”所有消费者都必须加 Redisson 锁”，而是统一要求：消息体字段清晰、状态先落库、异常不要吞、失败交给 RocketMQ 重试、DLQ 必须有兜底、缓存要及时清理。这样后续让 AI 辅助写消费者时，它不会随便 catch 异常导致 MQ 重试失效，也不会漏掉失败态处理。

## MQ 消费者开发规范

## 背景
本项目 AI 分析任务通过 RocketMQ 异步消费，当前主链路是 video-analysis-topic + video-group，失败兜底是 %DLQ%video-group。
每个消费者必须严格遵循以下流程，不允许跳步或改变顺序。

## 标准六步流程
1. 解析消息体，提取 mediaId 和 action
2. 查询 MySQL media_files，任务不存在则直接 return
3. 先把 ai_summary 更新为“处理中/消费者已接单”，让前端轮询能看到状态变化
4. 执行核心业务逻辑：FFmpeg 提取音频 → 临时音频上传 MinIO → ASR 转写 → DeepSeek 总结 → 保存转写和总结
5. 成功后清理 media:list:user:{userId} 缓存，让前端下一次轮询读到新结果
6. 任何三方 API 或落库异常都向上抛出，交给 RocketMQ 重试

## 异常处理规范
- 核心业务逻辑中的异常必须抛出，不允许catch后吞掉
- 抛出异常后由 RocketMQ 自动重试，maxReconsumeTimes=3
- 三次 MQ 重试均失败后转入死信队列，由 VideoAnalysisDlqConsumer 标记任务失败
- 如果是 Token 额度已耗尽，不抛异常，直接写入“今日 AI 算力已耗尽”并 return，避免无意义重试

## 禁止事项
- 禁止在消费者中随意吞异常，否则 RocketMQ 无法感知失败
- 禁止把前端交互逻辑写进消费者



Q12: 数据库
当前代码里核心表不是 video_task_record，而是 media_files、users、ai_summary_result 这几类。
1. users（用户表）
- 核心字段：id、username、password 等，主要用于登录和前端按 userId 隔离数据。
- Token 日额度没有直接落 MySQL，而是通过 Redis/Redisson 记录 user:token:usage:{userId}，因为这是高频、带 TTL 的计数型数据，放 Redis 更合适。
2. media_files（视频与任务主表）
- 核心字段：id、user_id、filename、file_path、status、ai_summary、transcript_text、cover_url、file_md5、file_size、chunk_count、upload_time。
- 面试话术：这是上传、展示和 AI 分析状态的主表。file_md5 字段配合唯一索引 idx_file_md5 实现秒传和存储去重；file_path 指向 MinIO 中的完整视频；ai_summary 和 transcript_text 承载前端轮询结果。当前 status 字段相对简单，更多状态是通过 ai_summary 文案表达，例如“已进入消息队列”“处理中”“失败”等。如果要继续工程化，可以把 AI 分析状态拆成独立枚举字段。

[图片]

3. ai_summary_result（AI 分析结果表）
- 核心字段：id、media_id、user_id、result_json、created_at，并建立 media_id / user_id 索引。
- 面试话术：media_files.ai_summary 是为了前端轮询和列表展示方便，ai_summary_result 则用于独立沉淀结构化 AI 结果。大模型返回的时间轴 JSON 后续可能扩展成章节、思维导图、问答索引等，把它独立出来可以降低主表变更频率，也方便后续做分析结果历史版本或审计。

- "如果存数据库，可以用TEXT或者MEDIUM TEXT类型"
- "但我们项目已经有MinIO了，直接用对象存储更合适"

建数据库表，考虑一些什么？
第一，遵守基础规范。 
给每一张表都标配上逻辑删除字段。 create_time、update_time 和 is_deleted。同时，严格控制字段类型，比如用 TINYINT 存储视频任务的各种状态，绝不无脑滥用 VARCHAR。
第二，根据业务的真实查询逻辑加索引。 建表绝不能脱离代码逻辑。
在 DoVideoAI 中，用户需要经常查看自己的视频记录，所以必须在 user_id 上建普通索引。平台上有一个核心功能是“用户查看自己的历史视频记录”，所以我直接建了 (user_id, create_time) 的联合索引，这样拿到的数据天然就是按时间排好序的，彻底免去了内存排序的开销。
第三，大字段的剥离意识。 在学校做小项目很容易习惯一张表包揽一切。
但在你的视频项目里，AI 分析出的逐字稿和长文本体积非常大。建表时你考虑到了这点，没有把上万字的文本硬塞进流水主表里，选择单独建明细表或者存入 OSS 等外部系统。这能体现出你已经具备了初步的性能优化意识，知道保护主表的查询效率。



引入 RocketMQ 将视频处理相关长耗时任务全链路异步化，将 60s+ 上传接口响应时间压缩至 50ms 以内。
背景和流程
Q1: 背景痛点
视频处理本身就属于典型的长耗时、重算力业务。同步调用FFmpeg提取音频 或者 调用第三方API，链路耗时耗时较长时会达到1到3分钟。
1. 前端请求很容易发生 HTTP 超时，用户体验彻底崩溃。
2. 第二就是服务器资源容易被榨干。工作线程会被这种长耗时任务死死挂起，根本不需要太高并发，只要同时有十几个人上传视频，服务器的线程池就会被全部占满，导致整个系统对外的所有接口彻底瘫痪。

Q2: 具体实现：
- 分片合并上传成功后，用户点击 AI 分析，请求进入 /debug/ai。后端先用 Redisson 锁拦截同一 mediaId 的重复提交，再用 RRateLimiter 做 10次/min 全局限流，最后把包含 mediaId 和 action 的 AnalysisTaskMsg 投递到 video-analysis-topic。这一步不做 FFmpeg、ASR 和 LLM 调用，所以接口 RT 可以压缩到 50ms 级。
- 消费者 VideoAnalysisConsumer 专门监听这个 Topic。收到消息后解析 mediaId，先把 media_files.ai_summary 更新为“处理中”，再调用 AiService 执行完整分析链路。
- 真正的耗时链路在消费者里：FFmpeg 提取音频 -> 临时音频上传 MinIO -> 阿里 ASR 转写 -> DeepSeek 生成时间轴 JSON -> MySQL 落库 -> 清理 Redis 用户列表缓存。
- 前端状态闭环。前端在拿到“任务已投递”的响应后，开启定时器每 3 秒刷新 /media/list。当前端发现 aiSummary 已经不是“排队/处理中”文案，而是 Markdown 或可解析的 JSON 时间轴时，就停止轮询并渲染结果。

Q3: 异步选型：
在单机环境下，使用Java原生的线程池来处理异步任务无疑是最轻量、成本最低的方案。
我最终决定放弃本地线程池并引入RocketMQ，完全是基于AI视频解析这个特定业务场景的痛点做出的架构权衡。
- 第一点，视频转码是内存密集型任务，而JVM内存队列非常脆弱。
如果使用线程池，大量高价值的解析任务会积压在内存的阻塞队列中。一旦出现突发大文件导致JVM内存溢出，内存里的任务会全部永久丢失。
RocketMQ的物理落盘机制确保了即使崩溃，未处理的任务也绝对安全，重启后可以继续消费。
- 第二点，第三方API的不可控。调用大模型API非常容易遇到限流或网络超时。
如果依赖线程池处理重试逻辑，失败的线程需要进行休眠等待，这会迅速耗尽线程池的全部核心工作线程，导致后续所有正常用户的请求被直接拒绝。
引入RocketMQ后，遇到网络抖动只需向Broker返回重试状态即可。消息交由底层的阶梯延迟机制去处理，当前工作线程被立刻释放，做到了极其纯粹的资源隔离。
- 第三点，为未来的扩容铺路。
将耗算力的音视频处理服务和轻量级的Web接入接口耦合在同一个JVM进程里，本身就存在巨大的性能隐患。RocketMQ在这里充当了标准的物理缓冲层。面对未来可能的高并发压力，系统不需要重构任何核心代码，就可以直接将消费者逻辑剥离出去，独立部署为专用的高算力计算集群。

---
## RocketMQ 八股与项目

Q1：RocketMQ选型，vs Kafka  vs RabbitMQ
1. 首先是业务契合度与可靠性对比 Kafka：
Kafka 的架构设计追求极致的高吞吐量，其核心主在于大数据领域的日志场景。
但我的项目定位是面向用户的核心业务系统，更看重消息的可靠性以及可能会用到的复杂的业务能力。
RocketMQ 提供了极度丰富的业务级特性。例如 项目中实现应对网络抖动的指数退避重试，直接复用了 RocketMQ 原生的阶梯延迟消息与死信队列机制。未来系统若需拓展会员支付或高并发订单模块，其自带的分布式事务消息也能直接支撑，大幅降低了后续架构演进的成本。
2. 其次是海量消息堆积能力对比 RabbitMQ：
视频转码与大模型调用是极其典型的长耗时、重负载场景。
这就注定了消费者的处理速度必然远低于前端的生产速度，消息在 Broker 端发生大量堆积是常态。 RabbitMQ 在面对海量消息积压时的性能不好，极易引发服务雪崩。
而RocketMQ天然支持亿级别的消息堆积，性能比较好，并且在严重堆积的状态下依然能保持极其稳定的写入性能，完美契合这种重计算业务的缓冲与削峰需求。
3. 务实的一些原因：
RabbitMQ 底层由 Erlang 语言编写，Kafka 则依赖不完全由Java编写。而RocketMQ 是全部 Java 写的，这种技术栈同源出问题了也方便排查。
再有国内的社区RocketMQ的讨论度也很高，对于我去学习或排查问题更为方便。

---
Q2：消息积压：
首先，我要确认积压的原因。我会查看监控日志：是不是 Bug？ 比如消费者线程是不是卡死了，或者抛出异常导致一直重试。如果是异常导致，我会先修复 Bug。
如果不是BUG，那么消息积压最粗粒度的原因，只有两种：要么是发送变快了，要么是消费变慢了。
    消费者端优化
1. 要解决积压的问题，可以通过扩容消费端的实例数来提升总体的消费能力。
必须同时增加 Topic 的 MessageQueue（队列）数量。

  因为在 RocketMQ 中，消费者是基于队列分配的（默认平均分配算法），一个队列同一时间只能被一个消费者消费。如果队列只有 4 个，我加再多机器也没用。所以我必须把队列数也扩容到 10 个以上，确保每个消费者都有活干。
    生产者端优化
1. 控制生产速度：例如在生产者端设置限流机制，避免消息生产速度过快
2. 没办法的办法是，通过关闭一些不重要的业务，减少发送方发送的数据量，最低限度让系统还能正常运转，服务一些重要业务。

---
Q3：重复消费
[面试王 即简历的第二条实现。]
面试官好，关于 RocketMQ 的重复消费问题，我在项目中设计了一套完整的“幂等性处理机制”来解决。
首先，我非常清楚 RocketMQ 保证的是 At Least Once，在网络波动时重复投递是可能发生的，所以不能假设消息只会被消费一次。我的处理是“提交入口拦截 + 消费者锁 + 锁内二次校验 + 落库兜底”。

1. 提交入口防重复：/debug/ai 使用 Redisson RLock，key 是 lock:analyze:{mediaId}。用户连点时，只有第一个请求能完成限流、状态更新和 MQ 投递，后续请求会直接返回“任务提交中，请勿重复点击”。这把锁只保护提交阶段，MQ 发出去就释放。
2. 消费者端防重复：AiService.asyncAnalyze 再基于同一个 lock:analyze:{mediaId} 获取 Redisson 锁，使用 tryLock()，不指定固定租约时间，整个 FFmpeg、ASR、DeepSeek、写 MySQL 的处理期间都持有锁，WatchDog 自动续期。两条重复 MQ 消息先后到达时，第二条抢不到锁就直接 return，避免重复消耗 Token。
3. 锁内二次校验：消费者抢到锁后会再查一次 media_files。如果发现 ai_summary 已经不是“处理中/等待调度/[MQ]”这类中间态，说明上一个持有者已经处理完，就直接跳过，防止锁释放与消息重试之间的边界问题。
4. 落库兜底：上传链路用 file_md5 唯一索引解决同内容重复存储；AI 总结结果写入 media_files 和 ai_summary_result。后续如果要继续增强，可以给 ai_summary_result.media_id 增加唯一约束，进一步防止重复结果记录。

---
Q4：顺序消费
首先是顺序的保障。在真实业务中追求全局有序会彻底丧失中间件的高并发能力，我采用的是局部有序方案，也就是分区有序，在生产者和消费者两端落实。
第一步在生产者端。发消息时我自定义了一个接口，把订单ID或者MD5这种任务的唯一标识作为Key。通过Hash(订单ID)取模队列总数的算法，在数学逻辑上确保同一个订单的所有状态流转消息，绝对被精准投递到同一个MessageQueue里。
第二步在消费者端。消费者会去锁住当前的 MessageQueue。 确保同一时间，只有一个线程在处理这个队列里的消息。只有前一条消息消费成功并提交了，才会继续消费下一条。这样就严格保证了同个订单的处理顺序。
Q：为什么不用全局有序？
全局有序要求所有消息都发到唯一的一个队列，这会导致该队列成为系统的性能瓶颈，完全牺牲了 RocketMQ 的高并发特性。对于订单业务，只要保证单个订单内部有序就够了，没必要追求全局有序。

---
Q5：消息不丢失
RocketMQ 要保障消息不丢失，需要从生产、存储、消费三个阶段来分别保障：
生产阶段：使用 同步发送（Sync Send） 模式，并配置 失败重试机制。类似于ACK确认机制，只有收到 MQ 返回的 SEND_OK 确认才算发送成功，失败了会自动重试。
存储阶段：将 Broker 的刷盘策略配置为 同步刷盘（SYNC_FLUSH），并开启 主从同步复制。这样能保证消息写入磁盘并同步到从节点后才返回成功，防止单机故障或断电导致消息丢失。
消费阶段：使用 手动 ACK 机制。只有当我的视频下载、转码、入库全部逻辑执行完成，没有抛出任何异常后，才手动返回确认，这个消息是发送给brocker的，与其对应的就是自动ACK，防止消息没处理完就丢了。

其实三者的确认机制在底层逻辑都是相通的，只是因为处理的业务逻辑不同，所以采取了不同的叫法。
像生产阶段叫做同步发送，是因为他验证的是“网络传输成功”
消费阶段使用 手动 ACK 机制，是因为他验证的是“业务逻辑执行成功”
https://my.feishu.cn/sync/SPKcdgm7tsASGgbZFSTcBH8ynQh

---
并发兜底保障：
Q1: 数据如何测得？
这个数据是我在本地开发环境基准测试得出的对比结果。 这里的 60s 和 50ms 指的是接口响应时间 (RT)，而不是视频处理的总耗时。
1. 对照组（同步架构）： 在引入 RocketMQ 之前，系统的视频上传与处理是强耦合的。用户上传视频后，主线程会同步阻塞调用线程进行转码。 我实际跑了一个 1 分钟的视频转码，发现前端页面一直转圈，Postman 显示接口响应时间超过 60秒（甚至经常超时报错）。
2. 实验组（异步架构）： 引入 RocketMQ 后，我把‘转码’这个耗时动作剥离了出去。 现在接口只做两件事：① 保存上传记录到数据库；② 发送一条消息给 MQ。 这两步都是轻量级操作。 我用 JMeter 再次压测，发现接口在 30~50ms 内就直接返回了 200 OK。
结论： 虽然后台转码实际上还是跑了 60秒（用户需要稍后轮询状态），但对于上传接口来说，响应速度确实提升了 1000 倍以上，还解决了 HTTP 连接超时的问题。


Q2: Redis 令牌桶放行后，发 MQ 失败了怎么办？
场景： 在你的第二阶段，用户请求刚通过 Redis 令牌桶拿到执行资格，准备把任务投递给 RocketMQ 时网络断了。 
兜底策略： 必须在代码中捕获发送 MQ 过程的异常。在 catch 块中直接向前端返回“系统繁忙，请重试”的错误提示。由于令牌桶算法会按固定速率自动补充令牌，无需做死板的“加回”操作。核心是阻断错误流程，立刻释放 Tomcat 线程，并让前端知晓真实状态，避免用户死等。

Q3: 大模型调用成功了，但消费者写 MySQL 落盘时失败了怎么办？
场景： 第三阶段中，消费者花了十几秒甚至几十秒调用第三方 API，拿到了极其珍贵的解析文本，但在最后一步 UPDATE MySQL 状态时遇到数据库宕机。 
兜底策略： 绝对不能让耗费 Token 跑出来的数据白费。MySQL落盘再次交给了消息队列异步处理，代码中直接抛出特定异常，触发 RocketMQ 的阶梯延迟重试机制。若三次重试耗尽依然落库失败，消息会自动进入死信队列（DLQ）。系统设置专门的死信监听器，一旦捕获此类消息，立刻触发告警人工介入，并将包含完整解析结果的消息体转存到本地容灾日志中。后续由开发人员手动修复数据，死守昂贵的算力成本底线。
面试时会经常性的问一些项目的兜底问题，比如某个部分消息发送失败了怎么办，或者网络波动导致消息发送两次重复处理怎么办......这些问题都是非常相通的，不能说这个会换一个场景就不会了。因为任何发送关键信息的场景，面试官都有可能单拿出一处问。
- 问某个部分消息发送失败了：就想ACK确认+失败重试的那一套
- 问消息发送两次重复处理了：就想幂等性去重那一套
八股是活的，这些也都是完全可以复用的。。

Q4: 异步处理视频，怎么通知用户真正解析成功了？ 
在当前的极速响应架构下，只要校验通过且 MQ 投递成功，前端就会拿到任务 ID 并提示“正在解析”。这属于物理投递成功，业务并未到达终态。 
应对方案是前端拿到全局唯一的任务 ID 后，开启定时器进行轮询。后端直接根据任务 ID 去 MySQL 查状态。只有查到数据库状态变更为“已完成”时，后端才将真正的 AI 总结文本完整返回给前端渲染，完成最终的业务闭环。



基于 Redisson + WatchDog 实现 AI 分析双阶段任务级分布式锁，锁 key 均为 lock:analyze:{mediaId}。提交阶段防用户连点导致重复投递 MQ，消费者阶段覆盖完整长耗时处理链路，杜绝重复消息并发执行导致重复 ASR/LLM 调用。
背景痛点
1. AI 分析链路包含 FFmpeg 音频提取、ASR 转写和 DeepSeek 总结，会消耗大量时间和 Token 成本。
2. 接入的 DeepSeek 大模型 API 是按 Token 计费的，每一次调用都是真金白银。
在这种场景下，最怕两类重复：第一类是用户因为页面还没返回就连续点击“AI 分析”，导致同一个 mediaId 被重复投递到 RocketMQ；第二类是 RocketMQ 的 At Least Once 语义导致同一条消息被重复消费。重复不一定立刻报错，但会制造重复 ASR/LLM 调用风险，最终表现为重复烧 Token 和重复更新结果。
核心痛点：提交入口要做到“同一 mediaId 同一时刻只允许一次投递”，消费者入口要做到“同一 mediaId 同一时刻只允许一个消费者真正执行”。

选型问题
Q1: 为什么不采取本地JVM锁？
OpenAI 的 CEO：要为六个月甚至一年后的模型能力做开发，绝对不要基于今天的模型局限性去构建产品。
1. 视频处理本身就是一个长耗时高资源消耗的场景，未来并发量上去是一定要扩容的，如果在单机阶段写死了 JVM 级别的原生并发锁，未来扩容时，这些锁在跨服务器环境下就会彻底失效。我还需要将核心业务的并发控制逻辑推翻重写。而用未来无论横向增加多少台服务器，核心代码一行都不需要修改，各个节点连接同一个 Redis 就能实现全局的并发调度。
2. 此外我还有个务实的考量： 系统本身已经为了分片上传的状态记录以及令牌桶限流引入了 Redis。既然 Redis 已经是当前架构的必选基础设施，直接复用它来实现分布式锁没有任何额外的运维和部署成本，技术栈更加收敛。



Q2: 为什么不全部采取 Redis SETNX 锁？
我不是完全不用 SETNX。分片合并就是短临界区，代码里用 merge:lock:{md5} + 30 秒 TTL 的 SETNX 合并锁就够了。
但 AI 分析链路我选 Redisson，是因为它不只是简单 SETNX：它提供可重入锁、线程持有校验、Lua 原子加锁/解锁，以及 WatchDog 自动续期。提交链路里要做额度校验、限流、查库、更新状态、清缓存、投递 MQ；消费者链路里要跑 FFmpeg、ASR、DeepSeek 和 MySQL 落库，耗时更不可预估，所以消费者锁更依赖 WatchDog。



## Redisson 分布式锁

Q1: 实现流程
1. 首先加锁肯定要看幂等 key 的选取:
AI 分析提交锁用的是 mediaId，key 为 lock:analyze:{id}。因为这个锁保护的是“某条已入库视频记录的分析提交动作”，直接用 mediaId 最准确；内容级去重另由上传阶段的 file_md5 唯一索引负责，不混在这把锁里。
2. 提交阶段加锁：
在 /debug/ai 入口里，我调用 Redisson 的 tryLock(0, -1, TimeUnit.SECONDS)。
- 如果获取失败，说明同一个 mediaId 正在提交中，直接返回“任务提交中，请勿重复点击”。
- 只有获取成功，才继续做全局限流、查库、更新排队状态和投递 RocketMQ。MQ 投递完成后 finally 释放锁。
3. 消费阶段加锁：
在 AiService.asyncAnalyze 里，我再次基于同一个 lock:analyze:{mediaId} 调用 tryLock()。
- 如果获取失败，说明已有消费者正在处理同一任务，直接 return。
- 如果获取成功，就在锁内二次查 DB，确认任务仍是处理中/等待调度状态，然后执行 FFmpeg、ASR、DeepSeek、写 MySQL 和删缓存。
4. 长任务续期（WatchDog）：
消费者链路耗时不可控，可能持续几十秒甚至几分钟，所以不能写死 leaseTime。tryLock() 不指定固定租约时，Redisson 会自动启动 WatchDog（看门狗）机制：
- WatchDog本质就是后台的一个定时任务。默认每隔 10s 检查一下，如果任务还未完成，就自动把锁的过期时间重置为 30秒。
- 这可以解决“AI 分析还没跑完，锁先过期导致重复消费者进入”的问题。
5. 安全释放
业务执行成功或者抛出异常，都会调用 unlock() 释放锁。 并且 Redisson 内部有防死锁机制：如果服务器宕机，WatchDog 线程挂掉，不再续期，锁会在 30秒 后自动过期，不会造成死锁。

---
Q2: MD5设计
MD5 (Message-Digest Algorithm 5) 是一种密码散列函数。 在我的项目中，我利用它将任意长度的视频文件数据，计算出一个32位的唯一哈希值。
我选择它的原因是：
1. 它能作为文件的‘数字指纹’，只要文件内容有一比特的差异，生成的 MD5 值都完全不同。
2. 不仅能防止同一条消息重复消费，还能防止不同用户上传同一个视频造成的算力浪费
关于 MD5 哈希碰撞
可以使用MD5+时间戳的方式避免极小概率产生的哈希碰撞，但这样无法做到视频级去重。

---
Q3: WatchDog 机制
WatchDog 的原理其实是一个后台定时任务。
续期机制： 当我加锁成功且没有指定过期时间时，Redisson 会默认设置锁的超时时间为 30秒（LockWatchdogTimeout）。 WatchDog 会启动一个后台线程（TimeTask），每隔 10秒（也就是 1/3 的超时时间）去轮训试探一下是否持有锁，如果还在，就重置过期时间为 30秒。这样只要业务还在跑，锁就永远不过期。
宕机死锁问题（关键防御）： 绝对不会死锁。  这个后台定时任务和我的业务线程 JVM 是死死绑定的。如果我的服务器宕机/断电了，WatchDog 线程也就挂了，自然就不会再续期了。 Redis 里的锁等到 30秒 倒计时一结束，就会自动过期释放。其他节点就可以正常拿到锁了。

---
Q4: Redisson 底层是怎么保证原子性
Redisson 底层是利用 Redis 的 Lua 脚本 来保证原子性的。
Redisson 并没有分开发送，而是把‘判断锁是否存在’、‘设置 Hash 值’、‘设置过期时间’这所有的逻辑，都写在了一段 Lua 脚本 里，然后把整个脚本发给 Redis 执行。因为 Redis 是单线程模型。 当 Redis 执行一段 Lua 脚本时，它会把这个脚本看作一个整体。在脚本执行结束之前，Redis 绝对不会去执行其他客户端发来的命令。
无论并发有多高，在 Redis 看来，所有的抢锁请求都是排好队、一个接一个执行 Lua 脚本的。这就从底层物理上保证了操作的原子性，彻底杜绝了并发冲突。

---
内容级去重的前置兜底保障
Q1: 内容级去重在锁外还有别的保障机制吗？
1. 第一层保障是上传前置检查。
前端在上传前计算文件 MD5，请求 /media/chunk/check。后端先查 media_files.file_md5，如果命中，直接返回 completed 和已有 fileUrl/mediaId，实现秒传，不再重复上传和合并。
2. 第二层保障是合并阶段的物理兜底。
media_files.file_md5 建了唯一索引 idx_file_md5。即便两个请求同时合并同一个文件，/merge 也会先用 SETNX 合并锁挡住并发，再二次查库；极端情况下数据库唯一索引仍然能防止同一 MD5 写入多条完整文件记录。
3. 第三层保障是分析提交状态拦截。
用户点击 AI 分析时，/debug/ai 会查 media_files.ai_summary，如果发现任务已经“正在”处理，就直接返回“任务已在后台运行，无需重复提交”。这层防的是同一 mediaId 的重复分析提交。

---
Q2: 既然 MySQL 既能做前置拦截又能靠唯一索引做底层兜底，那为什么不一开始就全盘只用 MySQL，还要费劲引入 Redisson？
核心原因在于关系型数据库根本扛不住瞬时并发洪峰。面对前端异常或恶意脚本瞬间涌入的大量重复请求，如果完全依赖 MySQL 的唯一索引去硬抗并发冲突，海量的无效 INSERT 试探会迅速耗尽数据库连接池，拉高系统全局的响应延迟甚至导致整个服务宕机。
Redis 作为纯内存组件，其核心架构使命就是挡在脆弱的关系型数据库前面吸收并发火力。我们用 Redisson 分布式锁把瞬时并发的无效请求全部拦截在内存层，只放行唯一一个合法线程去执行极其昂贵的 AI 算力调用并最终与 MySQL 交互落盘。这套架构完美切分了职责边界：Redis 专门负责抗下高并发冲突，MySQL 专职负责历史状态存储与绝对的最终物理兜底。

---
采取分片上传与断点续传机制，通过 Redis 记录分片上传状态，解决弱网环境下的连接中断超时问题，确保GB级大文件在不稳定网络下的传输可靠性。
背景和流程
## 分片上传与断点续传

Q1: 背景痛点
在系统初期，核心业务场景包含处理动辄1GB以上的的专业课程长视频录像。
这里存在一个极其现实的物理痛点：就是校园网的弱网环境。网络信号极易发生抖动甚至瞬时物理断连。全盘重来的机制极大浪费了网络带宽，系统可用性和用户的体验都非常差。
[图片]
[图片]
[图片]


---
Q2: 具体实现
1. 切片 + 计算MD5
      前端读取大文件，按照 5MB 的固定大小进行切片。同时计算出整个大文件的 MD5 值作为全局唯一的身份标识 Identifier。后续的所有网络请求都会携带这个 MD5 以及当前分片的序号。
     前端每次请求都会携带三个关键参数：
- identifier：整个大文件的 MD5（身份证）。
- chunkNumber：当前分片的序号（比如 0, 1, 5...）。
- file：当前分片的二进制流。

2. Check请求（秒传 / 断点续传 / 新任务）
在正式上传前，前端会先发一个 /check 请求，携带文件MD5。后端通过 MD5 去 Redis 查询当前文件的状态，分三种情况处理：
已完成 → 直接返回秒传成功，前端跳过整个上传流程。
进行中（Redis中存在该MD5的分片记录）→ 后端从 Redis 的 Set 中取出已成功落盘的分片序号集合，以数组形式返回给前端。前端经过比对就知道该跳过哪些、从哪一片继续传，从而实现断点续传。
无记录 → 当作新任务，从第一个分片开始上传。
断点续传的触发场景：用户主动暂停后重新上传、页面刷新、浏览器关闭后重新进入。
无论哪种场景，前端都会先走这个check流程。

3. 并发上传分片（先落盘，后记账）
前端过滤掉已传分片后，携带三个关键参数（总MD5、分片序号、二进制流）并发发起请求，并发数控制在3个，避免浏览器连接数耗尽。
后端处理时，严格遵循 "先落盘，后记账" 的策略：
- 先将分片存入 MinIO 对象存储，后端存储分片时，会按照chunks/{md5}/{分片序号}的结构存储到对应的位置。并且每一个分片上传的时候，前端会将这个分片的MD5值一块发给后端。后端在收到分片数据之后，会重新算一遍MD5，对比一下，如果发现数据被破坏了(比如有比特翻转)，我们就会直接拒绝这个分片，告诉前端“这个分片坏了，请重传”，保证最终存下来的数据是没问题的。
- 只有 MinIO 保存成功后，后端才会去 Redis 的 Set 中记录该分片的序号。
这就保证了 Redis 里记录的一定是有效的，避免了"假上传"导致的数据不一致。

4. 前端触发合并（方案选型）
每次分片上传成功并更新 Redis 后，后端会校验当前已上传的分片总数是否等于预期的总分片数。
- 后端合并： 每次分片上传成功并更新 Redis 后，后端校验已上传分片总数是否等于预期总分片数，数量吻合就调用 MinIO 的 API (composeObject)  接口合并。
这个方案在并发场景下会翻车。 假设最后三个分片同时到达后端，三个独立运行的后台线程同时去查 Redis，都发现"刚好凑齐了"，结果三个线程同时向 MinIO 下达合并指令，直接导致底层状态机冲突报错。
- 前端合并。 前端发出所有分片请求后自己等着，等所有请求全部返回 200 OK 后，前端主动且唯一地调用一次后端的 /merge 接口，从根源上杜绝并发合并的问题。


后端的 /merge 接口处理流程：
1. 用 Redis SETNX 设置合并锁（merge:lock:{md5}），做幂等保护，只有抢到锁的请求才执行合并。还要检查数据库状态，发现状态已经是"已完成"，就直接返回"合并已完成"，不做任何实际操作
2. 校验 Redis 中已上传分片数是否等于总分片数，确认无误。
3. 调用 MinIO 的 composeObject API，在存储引擎内部将所有分片合并为完整文件。
4. 合并完成后，计算合并文件的MD5，与前端最初发来的全局MD5比对，确保100%无损。
5. 校验通过，在 MySQL 生成业务记录，任务状态标记为"已完成"。

---
Q3: 小细节追问
分片大小是5MB： MinIO底层API硬性规定合并分片的最小阈值就是5MB，小于这个值请求会被拒绝。这兼顾了网络开销，切得太碎会导致HTTP连接数爆炸，切得太大一旦失败重传的时间成本又太高。

合并时机： 前端确认所有分片请求都返回200后，主动调用/merge接口。后端的/merge接口先用SETNX设合并锁做幂等保护，再校验Redis中已上传分片数是否等于总分片数，确认无误后调用MinIO的API执行合并。

如何判断文件完整性： 当前代码做了三层校验。第一层是分片级 MD5 校验，每个分片上传时前端会带 chunkMd5，后端读取分片字节后计算实际 MD5，不一致直接拒绝。第二层是合并前校验 Redis Set 中的分片数等于 totalChunks，防止缺片合并。第三层是 MinIO composeObject 合并后，后端通过 computeObjectMd5() 流式读取完整对象计算 MD5，再和前端文件 MD5 比对；不一致就删除已合并对象并抛异常，避免坏文件入库。

MD5计算性能问题？（加分项）
GB级文件在前端计算MD5确实比较耗时。当前代码用 FileReader 按 2MB 块增量读取并更新 md5Progress，能展示“正在校验文件”的进度，但还没有放到 Web Worker，所以极大文件仍可能带来主线程压力。如果要进一步优化，我会把 MD5 计算迁移到 Web Worker，避免 UI 卡顿；抽样 hash 只能用于弱秒传预判，不能替代严格去重场景下的完整 MD5。

---

Q4: 记录上传进度的 Redis 数据结构及选型
我使用的是 Redis 的 Set（集合） 结构。
把文件的 MD5 作为 Key，把‘已上传成功的分片编号’（比如 0, 1, 2）存到这个 Set 里。
Key 的设计：upload:task:{file_md5}     Value 的设计：存储已成功上传的分片编号 chunkNumber

为什么选 Set？主要有两个原因：
1. 是天然的幂等去重能力（最重要）： 在弱网环境下，前端可能会重复发送同一个分片。Set 集合天然保证了元素的唯一性，不管前端发多少次‘第 1 片’，Redis 里永远只有一条记录，数据绝对干净。
2. 判断极快： 断点续传的核心是要快速告诉前端‘哪些还没传’。Set 的查找效率是 O(1)，我直接取出所有已传的编号返回给前端，前端一比对就知道从哪续传了。

---
## 分片上传兜底保障

Q1: 并发上传时分片丢失了怎么办？
上传过程中某个分片的HTTP请求失败了——网络断了、超时了、服务端报错了。这个分片没有落盘到MinIO，Redis Set里也就没有它的序号。
分两层应对：
1. 实时重试。 
某个分片失败后前端对它单独重试，最多3次，不影响其他分片的正常上传。大部分情况下重试就能解决。
2. 断点续传兜底。    
如果重试3次都失败了（大概率是网络彻底断了），前端就会暂停整个上传任务。用户下次回来只要再点击就可以直接触发断点续传。前端发 /check 请求，后端从 Redis Set 返回已成功的分片序号（比如 {0, 1, 4}）。前端用总分片列表 {0,1,2,...,9} 减去这个集合，得到差集 {2, 3, 5, 6, 7, 8, 9}，只补传这些缺失的分片。

Q2: Redis 和 MinIO 里的碎片怎么办？
僵尸任务处理问题。为了防止 Redis 内存泄漏和 MinIO 存储浪费，我设计了两层清理机制。
1. Redis 自动过期（TTL）：每次分片上传成功后，后端把 chunkIndex 写入 Redis Set upload:task:{md5}，并刷新 24 小时 TTL。如果用户传到一半断网跑路，这个 Key 会在 24 小时后自动消失。现在已经删掉了以前的 __init__ 占位符写法，Redis Key 在首片 SADD 时自然创建。
2. MinIO 分片清理：合并成功且完整文件 MD5 校验通过后，后端会删除 Redis 分片状态，并通过 aiTaskExecutor.execute(() -> minioUtils.removeChunks(...)) 异步清理 MinIO 里的 chunks/{md5}/{chunkIndex} 分片对象，不阻塞合并接口返回。
如果用户上传到一半彻底放弃，目前 Redis 状态会过期，但 MinIO 半截分片还需要后续补生命周期策略或定时清理任务。这一点面试要诚实说：已实现“合并成功后的分片清理”，未完成“废弃上传任务的 MinIO 垃圾自动清理”。

---
Q3: Redis 挂了怎么办？上传进度会丢吗？

如果是服务端自身的问题，比如说MinIO挂了，或者Redis短暂不可用，我们目前的做法是让前端去兜底。服务端会把错误返回给前端，前端那边的重试机制会自动拉起，等服务恢复后用户这边就能继续上传，不用重新选择文件什么的
前置: 限流和降级
1. 发现问题做防御：
可以基于Redis的调用成功率做监控，发现尝试长时间不可用，那么可认为Redis是挂了
2. 限流和降级
当Redis挂了，且监控已经识别发现，这时候就要考虑限流和降级。
首先说降级是指我们无法再调用Redis了，那么这个场景下的降级方案 可以改为调用数据库。
但数据库扛不住高并发，所以就需要在前置拦截加入限流。避免数据库压力过高
这里降级的实操的方案是怎么做呢？
是我们至少要在代码里写  当发现Redis挂了，那么调用数据库。  
例如至少有一个 if(Redis正常) 调用Redis else 调用MySQL的逻辑
甚至我们可以提前配置一个开关，当出现故障时，可以一键切换成备选方案。
3. 当然我们这个业务对Redis是强/重度依赖，就需要保证Redis的高可用，例如哨兵、集群等等

后置: 核心取决于 Redis 的持久化配置。
最坏情况（确实会丢） 如果 Redis 彻底宕机且磁盘数据损坏，或者持久化配置不严格（比如只开了 RDB 且还没触发快照），那么上传进度确实会丢失。
- 后果：前端重试时，发现 Redis 里没有记录，会判定为‘新任务’，用户需要从 0 开始重新上传。
- 我的思考：我认为在‘文件上传’这个非金融核心场景下，这种极端情况下的体验降级是可接受的。毕竟 Redis 挂掉是极小概率事件，为了这极小概率去引入复杂的 MySQL 实时同步，反而会拖慢高频的分片写入性能，得不偿失。
正常防御 为了尽量不丢，生产环境可以开启 AOF 持久化策略
- 这样即使 Redis 挂了，重启后也能通过重放 AOF 日志恢复 1 秒前的数据。绝大部分用户的上传进度是可以被找回的。
https://my.feishu.cn/sync/BhNNd3TNRsOXdkbLGrlcw3jynlh
当每个分片写入 MinIO 完成后，服务端在更新 Redis 分片状态的同时，也向 MySQL 写入一条分片记录。这条记录包含 uploadId、分片序号、字节范围、分片 MD5、写入时间。Redis 和 MySQL 同时维护了分片状态，形成双写备份。正常情况下只用 Redis 查询（因为速度快），但 Redis 数据丢失时，MySQL 作为备份数据源可以恢复出完整的分片状态。


Q4: 合并失败了怎么办？

合并失败（比如MinIO服务异常）时：
1. 释放合并锁。
2. 任务状态标记为"合并失败"。
3. 分片数据不删除——它们还在MinIO里，不需要重新上传。
4. 前端下次请求时，走check流程发现状态是"合并失败"且所有分片都在，可以直接重新触发 /merge，不需要用户重新上传任何东西。
如果反复合并失败（说明某个分片数据可能损坏了），后端可以逐个校验每个分片的大小是否等于预期值（除最后一个分片外都应该是5MB），定位出哪个分片有问题，只让前端重传那一个。
[图片]


[图片]
Q5:为什么用redis不用mysql存储分片状态：
我是用Redis来管理分片上传的临时状态的。因为分片上传属于高频写入，比如一个1GB的大文件可能会被切割成上百个甚至上千个分片，每上传一个分片，后端都要记录一下“这个分片的状态”。如果是用MySQL的话，MySQL的压力会特别大，而且这些数据都是临时的，合并完之后就没用了，不值得进库。
redis读写速度特别快
为什么选 Set？主要有两个原因：
1. 是天然的幂等去重能力（最重要）： 在弱网环境下，前端可能会重复发送同一个分片。Set 集合天然保证了元素的唯一性，不管前端发多少次‘第 1 片’，Redis 里永远只有一条记录，数据绝对干净。
2. 判断极快： 断点续传的核心是要快速告诉前端‘哪些还没传’。Set 的查找效率是 O(1)，我直接取出所有已传的编号返回给前端，前端一比对就知道从哪续传了。
Q6:弱网测试怎么做，怎么兜底

基于clumsy。先不加任何弱网参数，跑一遍基线请求，记录：分片上传成功率，平均上传耗时，重试次数

1. 上传链路：分片上传 + 断点续传 + 分片校验 + 幂等键，失败只重传坏片。
2. 检索链路：向量检索失败时降级到 BM25 或缓存答案；低置信度时直接拒答而不是硬答。
3. 生成链路：外部模型超时走重试（指数退避），超过阈值切备用模型或返回“处理中”异步回调。
4. 任务链路：状态机 PENDING/RUNNING/FAILED/SUCCESS，防重复执行，支持失败重放。
5. 所有重试必须带 idempotency key，避免重复扣资源或重复写结果。

Q7:动态分片策略怎么做
第一档：极小分片，1MB。 适用于弱网环境，比如 2G/3G 网络、信号不稳定的移动场景，或者 RTT 超过 500ms 的情况。1MB 的分片在慢启动阶段很快就能完成一轮往返，不容易触发超时。但缺点是分片数量会很多，管理成本增加。
第二档：标准分片，5MB。 这是默认档位，平衡了传输效率和可靠性。适合普通宽带和 4G 网络。
第三档：大分片，10MB 到 20MB。 适用于千兆宽带或者内网环境，可以把网络的吞吐能力全部用满，减少 HTTP 握手和请求头的开销。
前端在初始化上传时，依然要告诉服务端文件的总大小和文件的 MD5。服务端在响应初始化请求时，会返回当前建议的分片大小以及当前档位的状态。前端开始上传分片，每完成一个分片，把这个分片的大小和字节偏移量记录下来，同时上报服务端。服务端根据这个分片的上传结果，更新当前网络状况的评估，决定下一个分片用多大。上传继续进行，直到所有字节范围都被覆盖。
合并请求发送到服务端时，前端不需要告诉服务端有多少个分片，只需要告诉服务端正向哪个 MD5 的文件发起合并。服务端根据这个 MD5 去查找所有分片的元数据，按字节偏移排序，验证覆盖完整性，然后执行合并。
Q8.有一个场景，有一个大文件10GB，每个分片为5MB，有1000个线程并发执行分片上传，怎么设计;
10GB 的文件，每个分片大小是 5MB，这意味着要把这个文件切成 2048 个分片。1000 个并发线程听起来很吓人，但如果真的让 1000 个线程同时往服务器写数据，会出现三个严重问题：第一，服务器的网络连接数会爆炸，操作系统维护大量 TCP 连接本身就有开销；第二，后端的 IO 资源是有限的，同时接收 1000 个写入请求，磁盘 IO 根本扛不住，会造成大量排队；第三，这 2048 个分片如果被 1000 个线程同时抢着上传，会产生激烈的竞争，很多线程实际上在空转等分配任务，而不是真正在做上传工作。
前端：任务调度层
前端需要做好三件事。第一件事是文件分片，前端把 10GB 的文件按照 5MB 的大小切成 2048 个独立的数据块，每个块都有自己的序号。第二件事是并发控制，这是最关键的设计——前端维护一个调度器，虽然内部逻辑上管理着 1000 个任务，但调度器会严格控制同时真正在发送请求的线程数量，比如控制在 20 个并发，这样既能充分利用网络带宽，又不会给后端造成压力。
后端：接收服务层
后端的核心职责是接收分片数据并写入 MinIO 分布式存储。基于令牌桶限流，设置每秒请求上限。

Q9.文件上传到99%，网络断开了，怎么快速恢复;

断点续传的工作原理
断点续传的本质是一套"状态同步 + 差异对比"的机制。当前代码里前端不依赖 IndexedDB/localStorage 保存进度，而是重新计算文件 MD5 后请求 /media/chunk/check。后端根据 Redis Set upload:task:{md5} 返回已经上传成功的分片序号；如果 MySQL 的 file_md5 已经命中，则直接返回秒传完成。
前端拿到这个列表之后，和自己本地的已上传分片集合做一个对比，算出差集。差集就是需要重传的分片。前端只把这几个分片重新加入上传队列，完成后再次触发合并流程。整个过程对用户来说几乎是无感的，只需要等待最后这几个分片传完就行。
前端的自动重试策略
前端在检测到网络断开时，不会盲目重试，而是采用指数退避的策略。第一次失败后等待 1 秒重试，第二次失败后等待 2 秒，第三次失败后等待 4 秒，以此类推。同时前端会监听浏览器的 online 事件，一旦浏览器检测到网络恢复，前端立刻重新发起请求。这样既能保证在短暂网络抖动时快速恢复，又不会在真正断网时无意义地频繁重试浪费资源。
Q10.大量的人上传同一个文件，怎么保证存储不浪费;
具体做法是这样的：前端在用户选择文件之后，用 spark-md5 分块计算整个文件的 MD5，并展示 MD5 计算进度。然后把 MD5 发送给后端 /media/chunk/check。后端收到 MD5 后先查 media_files.file_md5，如果找到了，说明服务器上已经有完整数据，直接返回 completed，不再上传分片。
Q11:并发上传时，同一个文件被两个人同时发起上传，会发生什么？你是怎么处理的？
如果是没有上传过的文件：
当前没有在“上传每个分片”阶段设置 SETNX 上传锁。因为分片对象路径按 chunks/{md5}/{chunkIndex} 组织，同一个分片重复上传会覆盖到同一个对象位置，Redis Set 记录 chunkIndex 也天然去重。真正需要互斥的是最终合并阶段，所以 /merge 用 merge:lock:{md5} 做 SETNX 合并锁。
兜底保证：
media_files.file_md5 有唯一索引。两个用户都上传完并几乎同时合并时，先抢到 merge:lock:{md5} 的请求执行合并和入库；后来的请求会被合并锁挡住，或者在二次查 MySQL 时发现文件已存在并直接返回已有记录。
如果是上传成功的文件：
第一步就查 MySQL `media_files`，如果 `file_md5` 命中，直接返回秒传成功响应（`completed/deduplicated=true`），前端看到这个标志就不再上传。

---


## RRateLimiter 令牌桶限流

配合 Redisson RRateLimiter 实现 10次/min 全局限流，遏制恶意请求带来的高昂 API 调用成本，保障服务可用性。

技术选型
(对比cityhub选型
因为只有令牌桶算法能同时满足 限制平均速率 和 允许突发流量 这两个核心需求，用户体验最好。
1. 对比固定窗口（Fixed Window）：解决‘临界突发’风险 固定窗口太死板。如果我限制每秒 60 次，可能会出现‘第 0.9 秒来了 60 次，第 1.1 秒又来了 60 次’的情况。虽然每秒都没超标，但在 0.2 秒内瞬间涌入 120 次请求，容易把服务打挂。令牌桶是基于流速的，平滑地解决了这个问题。
2. 对比漏桶（Leaky Bucket）：解决‘响应迟钝’问题 漏桶强制请求像滴水一样匀速通过。如果现在服务器很空闲，突然来了一波正常请求，漏桶也会强行让它们排队，导致用户响应变慢。 而令牌桶允许‘攒令牌’。如果有库存，突发流量可以瞬间拿走令牌立即执行，不需要无意义的排队，这对互联网业务的用户体验至关重要。

---

具体流程
当前代码没有手写 Redis Hash + Lua 令牌桶，而是直接复用 Redisson RRateLimiter，少写一套容易出错的分布式限流组件。
具体实现流程如下：
1. Key 设计：limit:ai:global，表示 AI 分析入口的全局限流桶，不按单机区分。
2. 初始化规则：trySetRate(RateType.OVERALL, 10, 1, RateIntervalUnit.MINUTES)，也就是整个系统 1 分钟最多放行 10 次 AI 分析提交。
3. 请求到达：/debug/ai 拿到 Redisson RRateLimiter 后调用 tryAcquire(1)。如果拿到令牌，继续投递 RocketMQ；如果拿不到，直接返回“系统繁忙，请 1 分钟后再试”。
4. 为什么用 Redisson：它把限流状态放在 Redis，天然支持多实例共享；底层原子性由 Redisson 封装，业务代码只关注“拿不拿得到令牌”，可维护性比自己写 Lua 更高。
优化哪些方面？
- 当前是全局 10 次/min，适合保护成本；如果未来用户增多，可以拆成“全局限流 + 单用户限流”两层。
- 当前限流在提交入口，未来还可以在消费者侧根据 DeepSeek/ASR 平台的 TPM 或 QPS 做消费速率控制。

---
细节问题
Q1: Lua 脚本具体解决了什么问题？
如果是自己实现令牌桶，Lua 脚本的价值是把“读令牌数、计算补充、判断是否足够、扣减令牌、写回状态”打包成一次 Redis 原子操作，避免并发请求同时读到同一个剩余令牌导致限流被击穿。
但我当前项目没有手写这段 Lua，而是用 Redisson RRateLimiter。面试时我会说：我理解 Lua 方案的原理，但项目里优先选了成熟组件，降低自己实现分布式限流的风险。

---
Q2: 请求被限流了，前端怎么处理
有两类失败：Token 日额度耗尽会由 AiTokenQuotaInterceptor 抛 TokenQuotaExceededException，GlobalExceptionHandler 返回 HTTP 429；全局 RRateLimiter 没拿到令牌时，当前 /debug/ai 返回一段“系统繁忙，请 1 分钟后再试”的文本。前端 aiAnalyze 会检查 429、错误文本和“算力已耗尽”等关键字，展示提示并关闭 loading，不会一直轮询。

## 指数退避重试

设计指数退避重试机制应对三方 API 网络抖动，结合最终一致性保障，任务成功率极大提升。

背景痛点
调用第三方大模型 API 时常遇到网络波动或接口限流。直接失败对用户的体验很不好，我们宁愿多等几秒尝试增加任务成功的几率，就此引出了指数退避重试机制解决这个痛点。

Q1: 为什么采取指数退避而不是其他的时间梯度？
1. 立即重试。
报错的瞬间立刻再发请求。如果网络正在抖动，立刻发大概率还是报错，纯属白费力气。
2. 固定间隔重试。
强制约定每隔两秒重试一次。一旦第三方API大面积宕机，系统里所有失败的任务都会每隔两秒准时发起一波集体冲锋。这极易引发羊群效应，直接把对方服务器彻底打死，或者触发对方的风控安全机制，导致咱们的IP被拉黑。
3. 指数退避。
选择指数退避（比如等待1秒，2秒，4秒，8秒），本质上就是给对方服务器留出喘息恢复的时间。对方故障越久，咱们系统退避等待的时间就越长。这既不空耗咱们自己的线程算力，也避免了把下游第三方服务逼上绝路。

---

实现流程
我没有重复造轮子，是直接基于 RocketMQ 消费者原生的重试策略 来实现的，因为它天然就支持阶梯式的时间等待。
具体流程如下：
1. 异常捕获   如果调用大模型 API 出现网络抖动或超时，我会在业务代码的 catch 块中捕获异常，并直接向 MQ 返回稍后重试状态码。
2. 指数退避  一旦我返回了‘稍后重试’，RocketMQ Broker 会自动把这条消息挂起。 它会按照默认的 延迟级别 来进行重试：第一次失败等 10s，第二次 30s，第三次 1m... 时间逐级拉长，有效防止下游服务雪崩。
3. 最终一致性  我不能让它无限重试。我在配置文件里设置了 Max Reconsume Times = 3（最大重试 3 次）。 如果第 3 次还是失败，RocketMQ 会自动把这条消息扔进 DLQ（死信队列）。一旦有消息进来，立刻通知人工介入，从而保障数据的最终一致性。

---

兜底策略
Q1: 你怎么去解决幂等性？
你重试了 3 次，万一第一次其实调用成功了，只是因为网络超时没收到响应。你重试第二次，会不会导致重复扣款或者重复下单？
我在本地数据库层面做了兜底防守。 
具体做法是，我在业务表中针对任务流水号建立了唯一索引。假设极端情况下网络其实通了，但由于超时触发了 MQ 的第二次重试，导致系统重复写入，数据库会直接抛出唯一键冲突异常。 我在代码里专门捕获这个冲突异常，将其当做正常处理完毕，向 MQ 返回消费成功。这样就利用数据库层面的物理限制，确保了本地绝对不会产生重复的脏数据。

---
Q2: 你重试了 3 次还是失败怎么办
我不能让它无限重试。我在配置文件里设置了 Max Reconsume Times = 3（最大重试 3 次）。 如果第 3 次还是失败，RocketMQ 会自动把这条消息扔进 DLQ（死信队列）。一旦有消息进来，立刻通知人工介入，从而保障数据的最终一致性。

---
## Token 成本风控

自研 AiTokenQuotaInterceptor + Token 成本风控

背景痛点
AI 分析不是普通查询接口，真正贵的是 ASR/LLM 调用。用户连点、脚本刷接口或者重复消费都会带来真实 Token 成本，所以我做了两层风控：入口限流控制请求频率，TokenQuota 控制单用户日额度。

Q1: Token 额度控制的完整流程？
1. 前置拦截：WebConfig 把 AiTokenQuotaInterceptor 注册到 /debug/ai、/debug/ai-direct、/debug/transcribe。请求进来时，拦截器从 X-User-Id、userId 参数或 mediaId 反查用户，调用 tokenUsageService.hasQuota(userId)。
2. 超额熔断：如果用户当天用量超过 ai.token.daily-quota（配置默认 50000），抛 TokenQuotaExceededException，由 GlobalExceptionHandler 统一返回 HTTP 429，前端识别 429 后停止 loading 并提示用户。
3. 异步兜底：因为 /debug/ai 只是投递 MQ，真正消耗 Token 的地方在消费者里，所以 AiService.asyncAnalyze 内部也会再次调用 hasQuota。这样即使消息积压到第二天或者用户额度已耗尽，消费者也不会继续烧钱。
4. 实际记账：DeepSeekUtils 收到 DeepSeek 响应后解析 usage.total_tokens，通过 TokenUsageContext 取当前 userId，再调用 TokenUsageService.recordUsage。
5. Redis 计数：TokenUsageService 使用 Redisson RAtomicLong，key 为 user:token:usage:{userId}，addAndGet(totalTokens) 原子累加；如果是首次写入，就设置 24h TTL，实现日维度自动重置。

Q2: 为什么不用 MySQL 记录 Token 用量？
Token 计数是高频、原子累加、带过期时间的临时额度数据，Redis 更适合。MySQL 更适合存订单、账单、审计这种强持久化数据。当前项目的目标是防止日常使用把额度打爆，所以 Redisson RAtomicLong + TTL 足够轻量。如果未来商业化，可以再把每日汇总异步落 MySQL 做账单。

Q3: RAtomicLong 和 Redis INCRBY 有什么区别？
底层思路类似，都是 Redis 原子计数。区别是我没有直接手写 StringRedisTemplate.opsForValue().increment，而是用 Redisson RAtomicLong 封装。好处是 API 更贴近 Java 并发语义，和项目里的 RLock、RRateLimiter 同属 Redisson 体系，代码风格统一；同时 expire、isExists、get 等操作也更顺手。

Q4: 为什么还要 RRateLimiter，Token 日额度不够吗？
不够。Token 日额度解决的是“一个用户一天最多能花多少”，但它不能阻止一分钟内突然打进大量 AI 分析请求，把消费者和三方 API 打爆。RRateLimiter 解决的是瞬时流量问题，当前 key 是 limit:ai:global，配置 RateType.OVERALL，10 次/min。两者的关系是：RRateLimiter 防瞬时洪峰，TokenQuota 防日维度成本失控。

Q5: 前端 Token 进度条怎么实时展示？
前端登录后调用 /debug/token-usage?userId=xxx，后端读取 user:token:usage:{userId} 的 RAtomicLong，返回 used、dailyQuota、remaining。前端用 used / dailyQuota 计算 tokenPercent，超过 80% 和 95% 分别切换 warn/danger 样式，并且任务完成后会 fetchTokenUsage 刷新一次，页面还会定时刷新。

---
## 结构化 Prompt 与时间轴导览

结构化 Prompt + AI 时间轴导览

背景痛点
普通 AI 总结是一大段文本，用户看完总结仍然不知道该跳到视频哪一段。我的目标不是只生成“读后感”，而是把长视频变成可以按知识点点击跳转的导览，所以必须让模型输出机器可解析的结构化结果。

Q1: 整体实现流程？
消费者先用 FFmpeg 提取音频，再把临时 mp3 上传到 MinIO 并生成预签名 URL，交给阿里 ASR 转写。拿到转写文本后，DeepSeekUtils 在 systemPrompt 中要求模型输出严格 JSON 数组，每个元素包含 startTime、topic、summary。AiService 把结果保存到 media_files.ai_summary，同时通过 AiSummaryResultService 保存到 ai_summary_result.result_json。前端轮询拿到 aiSummary 后，parseTimeline 截取 JSON 数组并 JSON.parse，渲染为时间轴节点。用户点击节点后，前端设置 videoElement.currentTime = startTime 并播放。

Q2: Prompt 怎么约束模型输出？
我在 systemPrompt 里做了三类约束：
1. 角色约束：把模型设定为专业视频内容分析专家。
2. 字段约束：要求输出 JSON 数组，字段是 startTime、topic、summary，并要求 startTime 必须来自章节第一句话的开始秒数，不允许编造。
3. 示例约束：直接给出数组示例，让模型知道字段名和数据类型，降低输出 Markdown、寒暄文本或错误字段名的概率。

Q3: 为什么字段用 startTime/topic/summary？
startTime 是交互的关键，前端要靠它驱动 videoElement.currentTime；topic 是时间轴节点标题，用于快速扫读；summary 是该片段的短摘要，控制在较短长度内，避免时间轴列表变成大段文章。这三个字段刚好覆盖“跳转、识别、理解”三个动作。

Q4: 如果模型输出不规范怎么办？
前端 parseTimeline 做了容错：先去掉 ```json 代码块包裹，再截取第一个 [ 到最后一个 ]，尝试 JSON.parse；如果解析失败或数组为空，就不渲染时间轴，降级展示 Markdown 总结内容。也就是说结构化解析失败不会白屏。后续如果要更强，可以在后端增加 JSON Schema 校验，失败时让模型重试一次。

Q5: 时间戳准确性怎么保证？
Prompt 明确要求 startTime 必须等于转写文本中该章节第一句话的开始秒数，不允许模型自行估算。准确性的前提是 ASR 转写文本本身要带时间信息。如果 ASR 返回的时间戳粒度不够，模型只能基于文本上下文组织章节，时间轴就会有误差。后续优化方向是保存更细粒度的 ASR 分句时间戳，甚至用 SRT/VTT 结构直接生成时间轴。

Q6: 为什么结果既写 media_files.ai_summary，又写 ai_summary_result？
media_files.ai_summary 是为了前端列表和轮询方便，用户刷列表时可以直接拿到当前结果。ai_summary_result.result_json 是为了沉淀 AI 结构化结果，后续如果要做版本管理、质量评估、导出、RAG 索引，都不应该把所有分析结果塞在主表里。

Q7: Function Calling / Redis 会话记忆 / RAG 实现了吗？
没有。当前项目真实落地的是结构化 Prompt + 时间轴导览，不是多轮问答。面试时如果被问到 RAG，我会说这是后续演进：把 transcriptText 按时间切片，做 embedding 存向量库，用户追问时召回相关片段再喂给模型。

## MinIO 对象存储

Q1: 视频文件存在 MinIO，处理完会删除吗？这就一直存着？（成本意识）
这涉及成本控制问题。原始视频文件动辄几百兆，长期存储成本太高。
当前真实代码里，完整视频会保存在 MinIO 的 media bucket 中，用户删除记录时才会调用 minioUtils.removeFile 删除对应对象。AI 分析过程中临时生成的 mp3 会上传到 MinIO 供 ASR 访问，处理完成后 finally 里会调用 removeByUrl 清理临时音频；分片合并完成后也会异步 removeChunks 清理分片对象。
所以面试时不要说“已经配置 MinIO Lifecycle 24 小时自动删除”，代码里没有这个配置。更准确的说法是：当前已实现业务侧主动清理临时音频和分片，原始视频按用户删除触发清理；如果上线后存储压力变大，可以再补 MinIO Lifecycle 或对象存储生命周期策略。

---
Q2: 你的 MinIO 部署在哪里？磁盘满了怎么办？
目前我是通过 Docker  部署的单机版 MinIO
关于磁盘和扩展性，我有两手准备：
软清理：当前代码已经清理 AI 分析临时音频和合并后的分片；原始视频由用户删除触发清理。后续可以补生命周期规则，按业务保留期自动删除长期不用的视频。
硬扩展（S3 协议优势）： MinIO 是完全兼容 AWS S3 协议的。 如果未来用户量暴增，本地磁盘扛不住了，我可以在不改动一行 Java 代码的情况下，直接把配置文件的 Endpoint 改成阿里云 OSS 的地址。 这种云原生的设计，保证了我的存储层可以无限水平扩展。

---
Q3: 你简历里写了“实现音视频文件与业务服务的物理隔离”，这是什么意思？为什么要这么做？为什么不直接存在后端服务器的本地磁盘里？
如果我把文件直接保存在后端服务器（Tomcat）的本地磁盘（如 /home/upload），系统扩展性很差。
所谓‘无状态化’，我是这么理解的： 假如我的业务火了，我需要把后端扩容到 3 台服务器（Server A, B, C）。
- 用户 1 把图片上传到了 Server A 的硬盘。
- 下次请求被负载均衡分发到了 Server B。
- 后果：Server B 的硬盘里没有那个文件，用户就访问不到了。
所以我引入 MinIO 做‘物理隔离’： 我把存储层单独剥离出来，让所有的后端节点（A, B, C）都去连接同一个 MinIO 集群。 这样无论后端怎么扩容、重启，都不会丢失数据。这就是我简历里说的**‘保障系统的无状态化扩展能力’**。”

---
Q4: 你说“保障数据隐私”，MinIO 里的文件是公开的吗？如果有人拿到了文件的 URL，是不是随便谁都能下载？
背景：考察你懂不懂 Pre-signed URL（预签名）。如果你说“我设了密码”，那就太 Low 了。
当前已经把访问方式改成预签名 URL：MinIOConfig 不再设置 bucket public policy，bucket 默认保持 private；MinioUtils 新增 getPresignedUrl(objectName, expirySeconds)，底层调用 MinIO SDK 的 getPresignedObjectUrl。
前端不再直接把 media_files.filePath 当 video src，而是在时间轴播放前请求 GET /media/access/{id}。后端根据 mediaId 查 media_files，解析 filePath 得到 objectName，再生成 1 小时有效的预签名 URL 返回给前端。AI 分析里的临时 mp3 也通过 uploadTempAudio 上传后生成 1 小时预签名 URL，供 ASR 服务拉取。
严格说当前 /media/access/{id} 还没有强鉴权，只是提供了私有桶 + 短期 URL 的访问模型；如果上线公网，下一步要在这个接口里校验 userId 和 media_files.user_id 归属关系。



## 前端轮询

Q1: 你这个”即时响应 + 延迟轮询”机制具体是怎么做的？
这个机制的设计初衷，是让 AI 分析接口快速返回，避免 HTTP 长连接一直等待 FFmpeg、ASR 和 LLM。
第一步：即时响应。用户点击 AI 分析后，/debug/ai 完成 Redisson 防重复提交、RRateLimiter 限流、Token 额度校验、更新 ai_summary 为排队状态、投递 RocketMQ，然后立即返回“任务已投递”。
第二步：异步处理。VideoAnalysisConsumer 后台消费消息，处理完成后把 transcript_text 和 ai_summary 更新到 media_files，并清理 media:list:user:{userId} 这个 Redis 列表缓存。
第三步：前端轮询。前端 startPolling 每 3 秒调用 fetchList，也就是请求 /media/list?userId=...&_t=timestamp。后端先查 media:list:user:{userId} 缓存，未命中则查 MySQL。前端拿到列表后找到当前 mediaId，判断 aiSummary 是否包含 Markdown 标题或可解析的时间轴 JSON，成功或失败都停止轮询。

---
Q2: 如果后台转码失败了（比如文件损坏），前端会一直显示“处理中”转圈吗？怎么处理？
不会一直转圈。主消费者不吞异常，异常会交给 RocketMQ 重试；如果 maxReconsumeTimes=3 后仍失败，消息进入 %DLQ%video-group，VideoAnalysisDlqConsumer 会解析 mediaId，并把 media_files.ai_summary 更新为“AI 分析失败，已重试 N 次”等失败文案。前端轮询时检测到“失败/Error/超时/500”等关键词，就会停止轮询并提示用户。
另外前端还有 5 分钟兜底超时，会清理 pollingTimers，避免页面无限挂着。

---
Q3: 为什么用轮询，用户如果非常多同时上传不炸了吗？
这就涉及到了技术选型中的成本与收益权衡（Trade-off）。
确实，如果并发量级真的无限放大（比如亿级），轮询会浪费带宽。但在当前的业务规模下，我选择‘延迟轮询’而不是‘WebSocket’，是基于以下三点深层考量：
1. 架构复杂度对比（ROI 分析）
- WebSocket（长连接）：虽然实时性好，但它是有状态的。我们需要维护数万个长连接的心跳，经过 Nginx、网关时还需要配置保持连接，且发生网络抖动时重连机制很复杂，运维成本极高。
- 轮询（短连接）：它是无状态的，完全符合 HTTP 协议特性。开发简单、调试容易，不需要专门的网关支持。
- 结论：在业务起步阶段，为了节省 3 秒的延迟去引入一套复杂的 WebSocket 架构，属于过度设计（Overkill）。
2. 资源消耗的“错峰”考量
- 当前轮询查的是 /media/list，返回用户的视频列表，不是只返回一个 PROCESSING 单词，所以它不是最省流量的方案。
- 但项目规模较小，而且后端有 media:list:user:{userId} 30 分钟缓存，消费者完成后会主动删除缓存，能减少反复查 MySQL 的压力。
- 前端加了 _t 时间戳强制刷新，这是为了避免浏览器缓存影响状态感知；真正的服务端缓存仍由 Redis 控制。
3. 未来的优化方案（Plan B）
- 当然，我也考虑过如果用户量真的暴增到百万级，轮询确实会不行。
- 到时候我会升级为**‘自适应轮询’**：比如前 1 分钟每 3 秒查一次，1 分钟后每 10 秒查一次（因为视频还没转完大概率还要很久）。
- 或者在那时再引入 WebSocket 或 SSE (Server-Sent Events) 推送。
所以总结来说：选择轮询不是因为它性能最强，而是因为在当前场景下，它是最稳健、维护成本最低的方案。


## 简历逐条拷打

面试官大概率会指着你简历上的五条 bullet 逐条深挖。以下按简历原文顺序，每条给一组高频追问 + 关键话术。


### 第一条：5MB 分片上传与断点续传

> 简历原文：实现 5MB 分片上传与断点续传,前端计算文件 MD5 与分片 MD5,后端以 Redis Set 记录已上传分片并设置 24h TTL,分片先落 MinIO 再记账。合并阶段通过 Redis SETNX 合并锁 + MySQL file_md5 唯一索引兜底,避免重复合并与重复存储。

**Q1: 为什么是 5MB，不是 1MB 或 10MB？**

MinIO composeObject API 硬性要求除最后一片外，每个源对象必须 ≥ 5MB。5MB 是兼顾传输效率和失败重传成本的平衡点——切太碎 HTTP 连接数爆炸，切太大失败重传时间成本太高。最后一片可以小于 5MB，MinIO 不限制。

**Q2: 前端怎么知道哪些分片传过了？**

前端在正式上传前调 `POST /media/chunk/check`，把文件 MD5 发给后端。后端先查 MySQL `file_md5` 唯一索引——命中直接返回秒传；再查 Redis `SMEMBERS upload:task:{md5}` 返回已传分片序号集合。前端用总分片列表做差集，只传缺失的。

**Q3: "先落盘后记账"怎么保证一致性？**

分片先通过 `minioClient.putObject` 写入 MinIO `chunks/{md5}/{chunkIndex}`，成功后才 `SADD upload:task:{md5} chunkIndex`。如果 MinIO 写失败了，Redis 里就没有这个分片序号，前端下次 `check` 会重新传。不存在"Redis 说已传但 MinIO 里没有"的假象。

**Q4: 秒传怎么实现的？**

`media_files` 表有 `file_md5` 唯一索引。`check` 接口第一条 SQL 就是 `SELECT * FROM media_files WHERE file_md5 = ?`，命中直接返回 `{status:"completed", deduplicated:true, fileUrl, mediaId}`，前端跳过所有上传步骤。

**Q5: 两个人同时上传同一个文件怎么办？**

分片对象路径是 `chunks/{md5}/{chunkIndex}`，两个人写同一个位置不会冲突（后写覆盖）。真正需要互斥的是合并阶段——`/merge` 接口用 `SETNX merge:lock:{md5}` 做 30 秒合并锁，只有一个人能抢到。后来者要么被锁挡住，要么在二次查 MySQL 时发现记录已存在直接返回。`file_md5` 唯一索引是最终物理兜底。


### 第二条：RocketMQ 异步化

> 简历原文：引入 RocketMQ 将语音转写与 AI 推理链路异步化,AI 分析提交接口只负责加锁、限流、更新排队状态并投递消息,接口响应从 60s+ 压缩至 50ms 级。消费者处理 FFmpeg 音频提取、ASR 转写与 DeepSeek 总结,异常抛给 RocketMQ 最大 3 次重试,失败后进入 DLQ 并标记任务失败。

**Q1: 为什么是 RocketMQ，不是 RabbitMQ 或 Kafka？**

RabbitMQ 积压能力弱，消息堆积到百万级性能断崖下降，不适合视频这种长耗时任务可能大量积压的场景。Kafka 擅长海量日志流式吞吐，但延迟消息和死信队列需要额外开发。RocketMQ 原生支持 18 级阶梯延迟消息和 DLQ，刚好覆盖"失败重试 3 次→自动进死信"的需求，跟阿里技术栈也一致。

**Q2: 消费者拿到消息后具体做了什么？**

先更新 `ai_summary` 为"处理中"让前端轮询有感知 → 抢 Redisson 锁防重复消费 → 查 Token 额度 → FFmpeg 提取 mp3 → 上传 MinIO 生成预签名 URL → 阿里 ASR 转写 → DeepSeek 生成时间轴 JSON → 写 MySQL → 删 Redis 列表缓存 → 释放锁。任何一步失败抛异常，RocketMQ 自动重试。

**Q3: 消息丢了怎么办？**

RocketMQ 有同步刷盘 + 主从复制机制。我们 producer 用同步发送，broker 配置 `flushDiskType=SYNC_FLUSH`，consumer 处理成功才返回 `CONSUME_SUCCESS`。只要这三段都配好，消息不会丢。

**Q4: 消息积压了怎么办？**

当前项目用户量小不太可能积压。但如果有大量视频同时提交：第一，`/debug/ai` 入口的 RRateLimiter 10次/min 本身就是闸门，不可能瞬间涌入大量消息；第二，如果真的积压，可以临时增加 consumer 实例数量（RocketMQ 的 Queue 支持横向扩展），或者起一个专门消费积压消息的临时 consumer group。


### 第三条：Redisson + WatchDog 双阶段任务锁

> 简历原文：基于 Redisson + WatchDog 实现双阶段任务锁：提交阶段在 /debug/ai 使用 tryLock(0, -1, TimeUnit.SECONDS) 防连点,投递 MQ 后立即释放；消费者阶段在 AiService.asyncAnalyze 使用 tryLock() 持有整个处理过程,由 WatchDog 自动续期,避免重复消息并发执行导致重复 ASR/LLM 调用。

**Q1: 为什么分两个阶段？**

两个阶段的锁守护不同的窗口期。提交阶段锁很短——从用户点按钮到 MQ 消息发出去，毫秒级，防的是同一个 mediaId 被连点投递两条消息。MQ 发完就释放。消费者阶段锁很长——覆盖 FFmpeg + ASR + LLM 整个链路，可能几分钟，防的是 MQ 重试消息或用户隔段时间重复提交导致两个消费者同时处理同一个 mediaId。

**Q2: 为什么消费者锁用 tryLock() 而不是 tryLock(0, -1, SECONDS)？**

`tryLock()` 不带任何参数 = 使用 WatchDog 默认 30s 超时 + 自动续期。消费者处理链路不可预估耗时（ASR 可能要几十秒，LLM 可能更久），不能用固定 leaseTime。WatchDog 每 10 秒续期一次，只要线程活着锁就不会过期。而提交阶段的锁不需要续期，MQ 发出就释放，所以用 `tryLock(0, -1, SECONDS)` 直接指定 leaseTime=-1 也可以，但那个锁的生命周期本来就很短。

**Q3: 两个阶段用同一个 lockKey 会有冲突吗？**

不会。两个 `tryLock` 都是在同一个 JVM 线程内串行执行，提交阶段拿到锁、发 MQ、释放锁之后，消费者才可能被触发。而且消费者和提交者通常不在同一个线程甚至不在同一个服务实例里，第一条锁释放后第二条才可能抢到。

**Q4: WatchDog 续期失败了怎么办？**

WatchDog 是 Redisson 内部的后台线程，底层靠 Redis 的 `PEXPIRE` 续期。如果 Redis 挂了导致续期失败，锁会在 30 秒后自动过期。这时如果另一个消费者拿到锁，会出现短暂的双消费者窗口。这就是为什么消费者拿到锁后还要**二次查 DB**——检查 `aiSummary` 是否已经包含完成结果，如果是就直接跳过。锁不是唯一的防线。


### 第四条：Token 成本风控

> 简历原文：自研 AiTokenQuotaInterceptor 拦截器,在 /debug/ai、/debug/ai-direct、/debug/transcribe 入口做用户日额度校验；DeepSeek 返回 usage 后通过 Redisson RAtomicLong 累计 user:token:usage:{userId},首次写入设置 24h TTL,超额时统一返回 429。配合 Redisson RRateLimiter 实现 AI 分析 10次/min 全局限流,前端 Token 进度条实时展示消耗。

**Q1: 日额度校验和全局限流有什么区别？**

两个维度：RRateLimiter 是**时间窗口**维度——"一分钟内整个系统只放 10 个请求"，防瞬发洪峰。Token 日额度是**用户成本**维度——"一个用户一天最多花 50000 Token"，防单个用户把预算烧穿。前者管频率，后者管总量。

**Q2: 为什么用 RAtomicLong 而不是 MySQL 计数？**

Token 计数是高频原子累加：每次 LLM 调用完都要 `addAndGet(totalTokens)`。MySQL 的 `UPDATE ... SET used = used + ?` 在这种高频场景下行锁竞争严重。Redisson RAtomicLong 底层是 Redis `INCRBY`，单线程模型天然无竞争，加上 `expire` 就能实现日维度自动清零。如果未来要做账单审计，可以再异步把每日汇总落 MySQL。

**Q3: 超额返回 429 是怎么实现的？**

AiTokenQuotaInterceptor 在 `preHandle` 里调 `tokenUsageService.hasQuota(userId)`，返回 false 就抛 `TokenQuotaExceededException`。GlobalExceptionHandler 捕获这个异常，设置 HTTP status 429，返回 JSON `{"code":429, "msg":"今日 AI 算力已耗尽"}`。前端 `fetch` 检测到 429 就停止 loading 并弹提示。消费者侧的 `asyncAnalyze` 里还有一次 `hasQuota` 检查——防止消息积压到第二天后被消费者继续烧钱。

**Q4: 为什么拦截器还注册了 /debug/ai-direct 和 /debug/transcribe？**

`/debug/ai-direct` 是跳过 MQ 直接异步执行 AI 分析的接口，`/debug/transcribe` 是单独提取文字的接口，都会产生 ASR 或 LLM 调用成本。如果拦截器只拦 `/debug/ai`，用户可以通过这两个接口绕过日额度限制。


### 第五条：结构化 Prompt + 时间轴导览

> 简历原文：通过结构化 Prompt 约束 DeepSeek 输出 startTime/topic/summary JSON 数组,后端持久化到 media_files.ai_summary 与 ai_summary_result.result_json,前端解析 JSON 渲染为可交互时间轴。点击节点驱动 videoElement.currentTime 跳转播放,将长视频转化为按知识点索引的导览体验。

**Q1: Prompt 里怎么保证模型一定输出合法 JSON？**

三层约束：第一，角色约束——告诉模型它是"专业视频内容分析专家"；第二，字段约束——明确要求 `startTime/topic/summary` 三个字段，且 `startTime` 必须来自转写文本第一句话的时间戳，不能编造；第三，示例约束——在 system prompt 里给出一个完整的 JSON 数组示例，让模型知道确切的数据格式。

**Q2: 模型真的输出非法 JSON 怎么办？**

前端 `parseTimeline` 做了容错：先 strip 掉可能的 ` ```json ` 包裹，截取第一个 `[` 到最后一个 `]`，然后 `JSON.parse`。解析失败或数组为空时不渲染时间轴，降级展示 Markdown 总结内容，不会白屏。后续可以加后端 JSON Schema 校验，失败让模型重试一次。

**Q3: 时间戳准吗？不准怎么办？**

准确性的前提是 ASR 转写文本带精确的时间信息。阿里 ASR paraformer-v2 返回的 sentences 数组包含 `begin_time` 和 `end_time`（毫秒级），后端把这些时间戳拼接在转写文本前面 `[0.0s - 2.5s] 大家好...`。DeepSeek 的 Prompt 要求它严格用第一句话的 `[开始秒数]` 作为 `startTime`。如果 ASR 本身的时间戳不准（比如音频质量问题），模型也只能基于不准的输入给出不准确的输出——这是一个上游依赖问题，后续优化方向是保存更细粒度的 ASR 分句时间戳。

**Q4: 为什么结果存了两张表？**

`media_files.ai_summary` 是给前端轮询和列表展示用的，跟 media_file 的业务状态绑定在一起。`ai_summary_result.result_json` 是独立的 AI 结果表，按 mediaId 关联，不污染主表。后续如果要版本管理（同一个视频多次分析保留历史）、质量评估、导出或 RAG 索引，直接从结果表取 JSON 就行，不需要动主表结构。

**Q5: 点击时间轴节点后视频怎么跳转？**

前端先调 `GET /media/access/{id}` 获取 1 小时有效的预签名视频 URL（MinIO bucket 是 private 的），设置 `videoElement.src = presignedUrl`。用户点击节点后 `videoElement.currentTime = startTime`，然后 `videoElement.play()`。整个视频不用重新加载，只是 `currentTime` 跳转。

**Q6: 后续怎么演进？**

一是把时间戳从 ASR 粒度做到更细的 SRT/VTT 字幕级，提高定位精度。二是引入 RAG——把 `transcript_text` 按时间切片做 embedding 存入向量库，用户可以在时间轴之外用自然语言追问"这个概念在哪里讲过"，系统先召回相关片段再让模型回答。
